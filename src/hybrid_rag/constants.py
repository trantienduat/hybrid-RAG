# Centralized default configurations for LLM and Embedding models
from hybrid_rag.config import app_config

DEFAULT_LLM_MODEL = app_config.llm_model
DEFAULT_EMBED_MODEL = app_config.embed_model
INDEX_SCHEMA_VERSION = 3

{
  "schema_version": 2,
  "dataset": {
    "name": "transformers-v5.9-answer-quality-v1",
    "source_identity": "git:https://github.com/huggingface/transformers.git@0a2757da521a7a49b8143d9e0c938f08747d682e:.",
    "source_package": "",
    "source_version": "",
    "source_artifact_sha256": "",
    "case_ids": [
      "transformers-t01",
      "transformers-t02",
      "transformers-t03",
      "transformers-t04",
      "transformers-t05",
      "transformers-t06",
      "transformers-t07",
      "transformers-t08",
      "transformers-t09",
      "transformers-t10"
    ]
  },
  "config": {
    "repository": "transformers-v5.9.0-bench-20260807",
    "answer_model": "gemma4:12b",
    "judge_model": "qwen2.5-coder:7b",
    "embedding_model": "nomic-embed-text",
    "top_k": 20,
    "context_n": 5,
    "repeats": 3,
    "seed": 42,
    "input_cost_per_million": null,
    "output_cost_per_million": null
  },
  "index": {
    "source_identity": "git:https://github.com/huggingface/transformers.git@0a2757da521a7a49b8143d9e0c938f08747d682e:.",
    "index_run_id": "ea222cfc-4015-4f74-86da-42d370e2d526",
    "index_schema_version": 3,
    "embedding_model": "nomic-embed-text"
  },
  "records": [
    {
      "case_id": "transformers-t01",
      "question": "What kinds of inputs can PreTrainedConfig.from_pretrained accept?",
      "reference_answer": "It accepts a model identifier hosted on Hugging Face, a directory containing a configuration saved with save_pretrained, or a path to a saved configuration JSON file. It also supports cache, download, local-only, token, and revision options.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Class] PreTrainedConfig (src/transformers/configuration_utils.py)\nGraph evidence: the Class PreTrainedConfig is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.configuration_utils.PreTrainedConfig).",
        "[Function] __call__PreTrainedConfig ()\nGraph evidence: the Function __call__PreTrainedConfig is connected by a graph relationship (node ID: __call__PreTrainedConfig).",
        "[Function] from_pretrained (src/transformers/modeling_utils.py)\nGraph evidence: the Function from_pretrained is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel.from_pretrained).",
        "[Function] from_pretrained (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Function from_pretrained is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase.from_pretrained)."
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedConfig.from_pretrained accepts the following inputs:\n\n*   **pretrained_model_name_or_path** (`str` or `os.PathLike`): Can be a model ID (string), a path to a directory containing a configuration file, or a path to a saved configuration JSON file.\n*   **cache_dir** (`str` or `os.PathLike`, optional).\n*   **force_download** (`bool`, optional).\n*   **local_files_only** (`bool`).\n*   **token** (`str` or `bool`, optional).\n*   **revision** (`str`, optional).\n*   **\\*\\*kwargs**: Additional keyword arguments.",
      "retrieval_latency_ms": 327.5579580804333,
      "generation_latency_ms": 101029.9713750137,
      "prompt_tokens": 898,
      "completion_tokens": 854,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8318701579598953,
      "answer_correctness": 0.8575209633567475
    },
    {
      "case_id": "transformers-t01",
      "question": "What kinds of inputs can PreTrainedConfig.from_pretrained accept?",
      "reference_answer": "It accepts a model identifier hosted on Hugging Face, a directory containing a configuration saved with save_pretrained, or a path to a saved configuration JSON file. It also supports cache, download, local-only, token, and revision options.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )"
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py"
      ],
      "answer": "The `PreTrainedConfig.from_pretrained` method accepts the following types of inputs:\n\n*   **`pretrained_model_name_or_path`**: Can be a string (the model id of a pretrained model configuration hosted on huggingface.co), a path to a directory containing a configuration file, or a path to a saved configuration JSON file.\n*   **`cache_dir`**: A `str`, `os.PathLike`, or `None`.\n*   **`force_download`**: A `bool`.\n*   **`local_files_only`**: A `bool`.\n*   **`token`**: A `str`, `bool`, or `None`.\n*   **`revision`**: A `str` (defaults to \"main\").\n*   **`**kwargs`**: Arbitrary keyword arguments.",
      "retrieval_latency_ms": 193.1147089926526,
      "generation_latency_ms": 155529.8710000934,
      "prompt_tokens": 2012,
      "completion_tokens": 1305,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9576340295236717,
      "answer_correctness": 0.7175119518609271
    },
    {
      "case_id": "transformers-t02",
      "question": "What does PreTrainedConfig.to_dict include when serializing a configuration?",
      "reference_answer": "It deep-copies the configuration attributes, adds the model_type when defined, records the Transformers version, and removes the unpacked kwargs entry before returning a dictionary.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Class] PreTrainedConfig (src/transformers/configuration_utils.py)\nGraph evidence: the Class PreTrainedConfig is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.configuration_utils.PreTrainedConfig).",
        "[Function] __call__PreTrainedConfig ()\nGraph evidence: the Function __call__PreTrainedConfig is connected by a graph relationship (node ID: __call__PreTrainedConfig).",
        "[Function] to_dict (src/transformers/pipelines/base.py)\nGraph evidence: the Function to_dict is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.pipelines.base.PipelineRegistry.to_dict)."
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/pipelines/base.py"
      ],
      "answer": "PreTrainedConfig.to_dict includes:\n*   A dictionary of all attributes that make up the configuration instance (from `self.__dict__`).\n*   The `model_type` (if available on the class).\n*   The `transformers_version`.\n*   Nested configurations (converted to dictionaries).\n*   `quantization_config` (included as a dictionary if it exists and is not already a dictionary).\n\nAdditionally, the method converts tuples to lists and removes \"kwargs\".",
      "retrieval_latency_ms": 766.343166003935,
      "generation_latency_ms": 97714.67474999372,
      "prompt_tokens": 1268,
      "completion_tokens": 809,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9097396015859399,
      "answer_correctness": 0.6019743719664987
    },
    {
      "case_id": "transformers-t02",
      "question": "What does PreTrainedConfig.to_dict include when serializing a configuration?",
      "reference_answer": "It deep-copies the configuration attributes, adds the model_type when defined, records the Transformers version, and removes the unpacked kwargs entry before returning a dictionary.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)"
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "It includes a dictionary of all the attributes that make up the configuration instance, the `model_type` (if available), and the `transformers_version`. Additionally, it processes nested `PreTrainedConfig` objects by converting them to dictionaries, converts tuples to lists, and handles the `quantization_config` if it exists.",
      "retrieval_latency_ms": 198.21716600563377,
      "generation_latency_ms": 105423.39529201854,
      "prompt_tokens": 2109,
      "completion_tokens": 884,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.6487978067152046,
      "answer_correctness": 0.5133015249043251
    },
    {
      "case_id": "transformers-t03",
      "question": "How does BatchEncoding indexing behave for string, integer, and slice keys?",
      "reference_answer": "A string key returns the corresponding value from the data dictionary, an integer returns the fast-tokenizer encoding for that batch item when encodings are available, and a slice returns sliced dictionary values.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Class] BatchEncoding (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class BatchEncoding is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.BatchEncoding)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "The behavior of `BatchEncoding` indexing depends on the type of the key provided:\n\n*   **String:** It returns the value of the dictionary associated with that key (e.g., 'input_ids', 'attention_mask').\n*   **Integer:** It returns the `tokenizers.Encoding` for the batch item at the specified index.\n*   **Slice:** It returns the values from the dictionary associated with that key, constrained by the slice.",
      "retrieval_latency_ms": 796.716709039174,
      "generation_latency_ms": 67921.28016694915,
      "prompt_tokens": 1380,
      "completion_tokens": 519,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8615426189177073,
      "answer_correctness": 0.7535824615241421
    },
    {
      "case_id": "transformers-t03",
      "question": "How does BatchEncoding indexing behave for string, integer, and slice keys?",
      "reference_answer": "A string key returns the corresponding value from the data dictionary, an integer returns the fast-tokenizer encoding for that batch item when encodings are available, and a slice returns sliced dictionary values.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_target (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded as target texts. Each sequence can be a string or a\n            list of strings (pretokenized string). If the sequences are provided as list of strings (pretokenized),\n            you must set `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair_target (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded as target texts. Each sequence can be a string or a\n            list of strings (pretokenized string). If the sequences are provided as list of strings (pretokenized),\n            you must set `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        tokenizer_kwargs (`dict[str, Any]`, *optional*):\n            Additional kwargs to pass to the tokenizer. These will be merged with the explicit parameters and\n            other kwargs, with explicit parameters taking precedence.\n    \"\"\"\n    # To avoid duplicating\n    all_kwargs = {\n        \"add_special_tokens\": add_special_tokens,\n        \"padding\": padding,\n        \"truncation\": truncation,\n        \"max_length\": max_length,\n        \"stride\": stride,\n        \"is_split_into_words\": is_split_into_words,\n        \"pad_to_multiple_of\": pad_to_multiple_of,\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "BatchEncoding indexing behaves as follows based on the type of the key provided:\n\n*   **String:** If the key is a string, it returns the value of the dictionary associated with that key (e.g., 'input_ids', 'attention_mask').\n*   **Integer:** If the key is an integer, it returns the `tokenizers.Encoding` for the batch item at that index (provided `self._encodings` is not None).\n*   **Slice:** If the key is a slice, it returns a dictionary where each value is the data associated with the key constrained by the specified slice.",
      "retrieval_latency_ms": 47.72366594988853,
      "generation_latency_ms": 81729.39779201988,
      "prompt_tokens": 1889,
      "completion_tokens": 602,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8844242643953568,
      "answer_correctness": 0.9573010678794238
    },
    {
      "case_id": "transformers-t04",
      "question": "What happens when BatchEncoding.convert_to_tensors is called with tensor_type=None?",
      "reference_answer": "It returns the BatchEncoding unchanged. When a tensor type is supplied, it converts the value to the corresponding TensorType and then performs the requested backend conversion.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Class] BatchEncoding (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class BatchEncoding is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.BatchEncoding)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "When `BatchEncoding.convert_to_tensors` is called with `tensor_type=None`, no modification is done and the method returns `self`.",
      "retrieval_latency_ms": 778.8481249008328,
      "generation_latency_ms": 49296.89429199789,
      "prompt_tokens": 1440,
      "completion_tokens": 344,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9879989514393515,
      "answer_correctness": 0.7876816686235437
    },
    {
      "case_id": "transformers-t04",
      "question": "What happens when BatchEncoding.convert_to_tensors is called with tensor_type=None?",
      "reference_answer": "It returns the BatchEncoding unchanged. When a tensor type is supplied, it converts the value to the corresponding TensorType and then performs the requested backend conversion.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "When `BatchEncoding.convert_to_tensors` is called with `tensor_type=None`, no modification is done and the method returns `self`.",
      "retrieval_latency_ms": 33.40466704685241,
      "generation_latency_ms": 41546.85995797627,
      "prompt_tokens": 2006,
      "completion_tokens": 339,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9879989514393515,
      "answer_correctness": 0.7876816686235437
    },
    {
      "case_id": "transformers-t05",
      "question": "How does PreTrainedTokenizerBase.add_tokens handle existing and special tokens?",
      "reference_answer": "It appends only tokens that are not already in the vocabulary. AddedToken objects can customize matching and whitespace behavior, and special_tokens changes normalization behavior. The method returns the number of tokens added.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Class] PreTrainedTokenizerBase (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class PreTrainedTokenizerBase is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase).",
        "[Function] _add_tokens (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Function _add_tokens is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase._add_tokens).",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Module] tokenization_utils_base (src/transformers/tokenization_utils_base.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module tokenization_utils_base."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedTokenizerBase.add_tokens ensures that `new_tokens` is a list or tuple and then calls `self._add_tokens(new_tokens, special_tokens=special_tokens)`. The specific logic for handling existing and special tokens is handled within the `_add_tokens` method.",
      "retrieval_latency_ms": 671.2579589802772,
      "generation_latency_ms": 169726.66891594417,
      "prompt_tokens": 926,
      "completion_tokens": 1575,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9382473888102069,
      "answer_correctness": 0.5396272155720324
    },
    {
      "case_id": "transformers-t05",
      "question": "How does PreTrainedTokenizerBase.add_tokens handle existing and special tokens?",
      "reference_answer": "It appends only tokens that are not already in the vocabulary. AddedToken objects can customize matching and whitespace behavior, and special_tokens changes normalization behavior. The method returns the number of tokens added.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedTokenizerBase.add_tokens checks if `new_tokens` is empty (returning 0 if so). If `new_tokens` is not a list or tuple, it converts it into one. It then calls the internal method `self._add_tokens(new_tokens, special_tokens=special_tokens)`.",
      "retrieval_latency_ms": 217.26666705217212,
      "generation_latency_ms": 126778.50958402269,
      "prompt_tokens": 2396,
      "completion_tokens": 1242,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9382473888102069,
      "answer_correctness": 0.648996146330117
    },
    {
      "case_id": "transformers-t06",
      "question": "What is the main responsibility of PreTrainedTokenizerBase.__call__?",
      "reference_answer": "It is the main tokenization entry point for one or more sequences or sequence pairs. It supports padding, truncation, overflow and offset options, optional target text, and returns a BatchEncoding.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Class] PreTrainedTokenizerBase (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class PreTrainedTokenizerBase is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase).",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Module] tokenization_utils_base (src/transformers/tokenization_utils_base.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module tokenization_utils_base.",
        "[Class] PushToHubMixin ()\nGraph evidence: the Class PushToHubMixin is connected by a INHERITS relationship (node ID: PushToHubMixin)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "The main responsibility of `PreTrainedTokenizerBase.__call__` is to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of sequences.",
      "retrieval_latency_ms": 757.0694999303669,
      "generation_latency_ms": 29836.11324999947,
      "prompt_tokens": 1011,
      "completion_tokens": 236,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.910347375615688,
      "answer_correctness": 0.5201651393801481
    },
    {
      "case_id": "transformers-t06",
      "question": "What is the main responsibility of PreTrainedTokenizerBase.__call__?",
      "reference_answer": "It is the main tokenization entry point for one or more sequences or sequence pairs. It supports padding, truncation, overflow and offset options, optional target text, and returns a BatchEncoding.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py"
      ],
      "answer": "The main responsibility of `PreTrainedTokenizerBase.__call__` is to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of sequences.",
      "retrieval_latency_ms": 35.03879101481289,
      "generation_latency_ms": 46283.03779207636,
      "prompt_tokens": 2507,
      "completion_tokens": 255,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.910347375615688,
      "answer_correctness": 0.5201651393801481
    },
    {
      "case_id": "transformers-t07",
      "question": "Which options change the parameter count returned by PreTrainedModel.num_parameters?",
      "reference_answer": "only_trainable restricts the count to parameters with gradients enabled, while exclude_embeddings removes parameters belonging to embedding modules from the count.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] num_parameters (src/transformers/modeling_utils.py)\n# [Function] ModuleUtilsMixin.num_parameters (src/transformers/modeling_utils.py:1028)\ndef num_parameters(self: \"PreTrainedModel\", only_trainable: bool = False, exclude_embeddings: bool = False) -> int:\n    \"\"\"\n    Get number of (optionally, trainable or non-embeddings) parameters in the module.\n\n    Args:\n        only_trainable (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of trainable parameters\n\n        exclude_embeddings (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of non-embeddings parameters\n\n    Returns:\n        `int`: The number of parameters.\n    \"\"\"\n\n    if exclude_embeddings:\n        embedding_param_names = [\n            f\"{name}.weight\" for name, module_type in self.named_modules() if isinstance(module_type, nn.Embedding)\n        ]\n\n    is_loaded_in_4bit = getattr(self, \"is_loaded_in_4bit\", False)\n    if is_loaded_in_4bit:\n        import bitsandbytes as bnb\n\n    total_params = 0\n    for name, param in self.named_parameters():\n        if exclude_embeddings and name in embedding_param_names:\n            continue\n        if param.requires_grad or not only_trainable:\n            # For 4bit models, we need to multiply the number of parameters by 2 as half of the parameters are\n            # used for the 4bit quantization (uint8 tensors are stored)\n            if is_loaded_in_4bit and isinstance(param, bnb.nn.Params4bit):\n                if hasattr(param, \"element_size\"):\n                    num_bytes = param.element_size()\n                elif hasattr(param, \"quant_storage\"):\n                    num_bytes = param.quant_storage.itemsize\n                else:\n                    num_bytes = 1\n                total_params += param.numel() * 2 * num_bytes\n            else:\n                total_params += param.numel()\n\n    return total_params",
        "[Class] PreTrainedModel (src/transformers/modeling_utils.py)\nGraph evidence: the Class PreTrainedModel is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel).",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] __call__num_parameters ()\nGraph evidence: the Function __call__num_parameters is connected by a graph relationship (node ID: __call__num_parameters).",
        "[Module] modeling_utils (src/transformers/modeling_utils.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module modeling_utils."
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py"
      ],
      "answer": "The options that change the parameter count returned by `PreTrainedModel.num_parameters` are:\n\n*   **only_trainable**: When set to `True`, it ensures only the number of trainable parameters is returned.\n*   **exclude_embeddings**: When set to `True`, it returns only the number of non-embeddings parameters.",
      "retrieval_latency_ms": 666.1415830021724,
      "generation_latency_ms": 53012.4041660456,
      "prompt_tokens": 1244,
      "completion_tokens": 489,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 0.6666666666666666,
      "answer_relevancy": 0.9933591938593486,
      "answer_correctness": 0.955539335910299
    },
    {
      "case_id": "transformers-t07",
      "question": "Which options change the parameter count returned by PreTrainedModel.num_parameters?",
      "reference_answer": "only_trainable restricts the count to parameters with gradients enabled, while exclude_embeddings removes parameters belonging to embedding modules from the count.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] num_parameters (src/transformers/modeling_utils.py)\n# [Function] ModuleUtilsMixin.num_parameters (src/transformers/modeling_utils.py:1028)\ndef num_parameters(self: \"PreTrainedModel\", only_trainable: bool = False, exclude_embeddings: bool = False) -> int:\n    \"\"\"\n    Get number of (optionally, trainable or non-embeddings) parameters in the module.\n\n    Args:\n        only_trainable (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of trainable parameters\n\n        exclude_embeddings (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of non-embeddings parameters\n\n    Returns:\n        `int`: The number of parameters.\n    \"\"\"\n\n    if exclude_embeddings:\n        embedding_param_names = [\n            f\"{name}.weight\" for name, module_type in self.named_modules() if isinstance(module_type, nn.Embedding)\n        ]\n\n    is_loaded_in_4bit = getattr(self, \"is_loaded_in_4bit\", False)\n    if is_loaded_in_4bit:\n        import bitsandbytes as bnb\n\n    total_params = 0\n    for name, param in self.named_parameters():\n        if exclude_embeddings and name in embedding_param_names:\n            continue\n        if param.requires_grad or not only_trainable:\n            # For 4bit models, we need to multiply the number of parameters by 2 as half of the parameters are\n            # used for the 4bit quantization (uint8 tensors are stored)\n            if is_loaded_in_4bit and isinstance(param, bnb.nn.Params4bit):\n                if hasattr(param, \"element_size\"):\n                    num_bytes = param.element_size()\n                elif hasattr(param, \"quant_storage\"):\n                    num_bytes = param.quant_storage.itemsize\n                else:\n                    num_bytes = 1\n                total_params += param.numel() * 2 * num_bytes\n            else:\n                total_params += param.numel()\n\n    return total_params",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )"
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py"
      ],
      "answer": "The options that change the parameter count returned by `PreTrainedModel.num_parameters` are:\n\n*   **`only_trainable`**: Determines whether to return only the number of trainable parameters (defaults to `False`).\n*   **`exclude_embeddings`**: Determines whether to return only the number of non-embeddings parameters (defaults to `False`).",
      "retrieval_latency_ms": 137.5001249834895,
      "generation_latency_ms": 53442.82066705637,
      "prompt_tokens": 2395,
      "completion_tokens": 343,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9796743072542734,
      "answer_correctness": 0.7044250015186374
    },
    {
      "case_id": "transformers-t08",
      "question": "How does resize_token_embeddings change a pretrained model?",
      "reference_answer": "It resizes the input embedding matrix when the requested vocabulary size differs, can pad to a multiple, optionally initializes added rows using the old embedding statistics, and re-ties weights through tie_weights when the model supports it.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,\n            where the generated tokens' probabilities won't be affected by the added embeddings because initializing the new embeddings with the\n            old embeddings' mean will reduce the kl-divergence between the next token probability before and after adding the new embeddings.\n            Refer to this article for more information: https://nlp.stanford.edu/~johnhew/vocab-expansion.html\n\n    Return:\n        `torch.nn.Embedding`: Pointer to the input tokens Embeddings Module of the model.\n    \"\"\"\n    model_embeds = self._resize_token_embeddings(new_num_tokens, pad_to_multiple_of, mean_resizing)\n    if new_num_tokens is None and pad_to_multiple_of is None:\n        return model_embeds\n\n    # Since we are basically reusing the same old embeddings with new weight values, gathering is required\n    is_quantized = hasattr(self, \"hf_quantizer\") and self.hf_quantizer is not None\n    if is_deepspeed_zero3_enabled() and not is_quantized:\n        import deepspeed\n\n        with deepspeed.zero.GatheredParameters(model_embeds.weight, modifier_rank=None):\n            vocab_size = model_embeds.weight.shape[0]\n    else:\n        vocab_size = model_embeds.weight.shape[0]\n\n    # Update base model and current model config.\n    self.config.get_text_config().vocab_size = vocab_size\n    self.vocab_size = vocab_size\n\n    # Tie weights again if needed\n    self.tie_weights()\n\n    return model_embeds",
        "[Function] _resize_token_embeddings (src/transformers/modeling_utils.py)\nGraph evidence: the Function _resize_token_embeddings is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel._resize_token_embeddings).",
        "[Class] PreTrainedModel (src/transformers/modeling_utils.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Class PreTrainedModel.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__hasattr ()\nGraph evidence: the Function __call__hasattr is connected by a CALLS relationship (node ID: __call__hasattr)."
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`resize_token_embeddings` changes a pretrained model by:\n1. Resizing the input tokens Embeddings Module (via `_resize_token_embeddings`).\n2. Updating the vocabulary size in both the model's configuration (`self.config.get_text_config().vocab_size`) and the model itself (`self.vocab_size`).\n3. Re-tying weights if needed using `self.tie_weights()`.",
      "retrieval_latency_ms": 633.1514170160517,
      "generation_latency_ms": 67323.91658297274,
      "prompt_tokens": 812,
      "completion_tokens": 708,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9717571695681501,
      "answer_correctness": 0.9576267846643417
    },
    {
      "case_id": "transformers-t08",
      "question": "How does resize_token_embeddings change a pretrained model?",
      "reference_answer": "It resizes the input embedding matrix when the requested vocabulary size differs, can pad to a multiple, optionally initializes added rows using the old embedding statistics, and re-ties weights through tie_weights when the model supports it.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,\n            where the generated tokens' probabilities won't be affected by the added embeddings because initializing the new embeddings with the\n            old embeddings' mean will reduce the kl-divergence between the next token probability before and after adding the new embeddings.\n            Refer to this article for more information: https://nlp.stanford.edu/~johnhew/vocab-expansion.html\n\n    Return:\n        `torch.nn.Embedding`: Pointer to the input tokens Embeddings Module of the model.\n    \"\"\"\n    model_embeds = self._resize_token_embeddings(new_num_tokens, pad_to_multiple_of, mean_resizing)\n    if new_num_tokens is None and pad_to_multiple_of is None:\n        return model_embeds\n\n    # Since we are basically reusing the same old embeddings with new weight values, gathering is required\n    is_quantized = hasattr(self, \"hf_quantizer\") and self.hf_quantizer is not None\n    if is_deepspeed_zero3_enabled() and not is_quantized:\n        import deepspeed\n\n        with deepspeed.zero.GatheredParameters(model_embeds.weight, modifier_rank=None):\n            vocab_size = model_embeds.weight.shape[0]\n    else:\n        vocab_size = model_embeds.weight.shape[0]\n\n    # Update base model and current model config.\n    self.config.get_text_config().vocab_size = vocab_size\n    self.vocab_size = vocab_size\n\n    # Tie weights again if needed\n    self.tie_weights()\n\n    return model_embeds",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,\n        \"return_special_tokens_mask\": return_special_tokens_mask,\n        \"return_offsets_mapping\": return_offsets_mapping,\n        \"return_length\": return_length,\n        \"split_special_tokens\": kwargs.pop(\"split_special_tokens\", self.split_special_tokens),\n        \"verbose\": verbose,\n    }\n\n    max_target_length = kwargs.pop(\"max_target_length\", None)\n\n    # First merge tokenizer_kwargs, then other kwargs (explicit params take precedence)\n    if tokenizer_kwargs is not None:\n        all_kwargs.update(tokenizer_kwargs)\n    all_kwargs.update(kwargs)\n    if text is None and text_target is None:\n        raise ValueError(\"You need to specify either `text` or `text_target`.\")\n\n    padding_strategy, truncation_strategy, max_length, kwargs = self._get_padding_truncation_strategies(\n        padding=all_kwargs.pop(\"padding\", False),\n        truncation=all_kwargs.pop(\"truncation\", None),\n        max_length=all_kwargs.pop(\"max_length\", None),\n        pad_to_multiple_of=all_kwargs.get(\"pad_to_multiple_of\"),\n        verbose=all_kwargs.get(\"verbose\", True),\n        **kwargs,\n    )\n\n    if text is not None:\n        # The context manager will send the inputs as normal texts and not text_target, but we shouldn't change the\n        # input mode in this case.\n        if not self._in_target_context_manager and hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n        encodings = self._encode_plus(\n            text=text,\n            text_pair=text_pair,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)\n\n    # When fp16/bf16 full eval is enabled, __init__ skips device placement so that\n    # evaluation_loop can cast dtype and move in one step. Move the model now for training.\n    if (args.fp16_full_eval or args.bf16_full_eval) and not self.is_model_parallel and self.model_init is None:\n        self._move_model_to_device(self.model, args.device)\n\n    # Activate gradient checkpointing if needed\n    if args.gradient_checkpointing:\n        self.model.gradient_checkpointing_enable(gradient_checkpointing_kwargs=args.gradient_checkpointing_kwargs)\n\n    # If the model uses a tokenizer, it may have a new tokens for fine-tuning purposes.\n    if isinstance(self.processing_class, (PreTrainedTokenizerBase, ProcessorMixin)) and hasattr(\n        self.model, \"config\"\n    ):\n        align_special_tokens(self.model, self.processing_class)\n\n    # Attach NEFTune hooks if necessary\n    if self.neftune_noise_alpha is not None:\n        self.neftune_hook_handle = activate_neftune(self.model, self.neftune_noise_alpha, self.accelerator)\n\n    # This might change the seed so needs to run first.\n    self._hp_search_setup(trial)\n\n    if DebugOption.UNDERFLOW_OVERFLOW in args.debug:\n        if args.n_gpu > 1:\n            # nn.DataParallel(model) replicates the model, creating new variables and module\n            # references registered here no longer work on other gpus, breaking the module\n            raise ValueError(\n                \"Currently --debug underflow_overflow is not supported under DP. Please use DDP with torchrun\"\n            )\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)"
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/trainer.py"
      ],
      "answer": "`resize_token_embeddings` changes a pretrained model by:\n1. Resizing the input tokens' Embeddings Module based on `new_num_tokens`.\n2. Updating the `vocab_size` in both the model's configuration (`self.config.get_text_config().vocab_size`) and the model itself (`self.vocab_size`).\n3. Re-tying weights using `self.tie_weights()`.",
      "retrieval_latency_ms": 117.72749992087483,
      "generation_latency_ms": 91138.11145804357,
      "prompt_tokens": 2376,
      "completion_tokens": 816,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8631756891794411,
      "answer_correctness": 0.7693360399949114
    },
    {
      "case_id": "transformers-t09",
      "question": "How does Pipeline.__call__ prepare batched or chat-style inputs?",
      "reference_answer": "It detects list, tuple, generator, or dataset-like inputs, converts valid message collections into Chat objects, chooses configured or default worker and batch sizes, and routes the data through a DataLoader and PipelineIterator stages.",
      "source_files": [
        "src/transformers/pipelines/base.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] get_iterator (src/transformers/pipelines/base.py)\n# [Function] Pipeline.get_iterator (src/transformers/pipelines/base.py:1169)\ndef get_iterator(\n    self, inputs, num_workers: int, batch_size: int, preprocess_params, forward_params, postprocess_params\n):\n    if isinstance(inputs, collections.abc.Sized):\n        dataset = PipelineDataset(inputs, self.preprocess, preprocess_params)\n    else:\n        if num_workers > 1:\n            logger.warning(\n                \"For iterable dataset using num_workers>1 is likely to result\"\n                \" in errors since everything is iterable, setting `num_workers=1`\"\n                \" to guarantee correctness.\"\n            )\n            num_workers = 1\n        dataset = PipelineIterator(inputs, self.preprocess, preprocess_params)\n    if \"TOKENIZERS_PARALLELISM\" not in os.environ:\n        logger.info(\"Disabling tokenizer parallelism, we're using DataLoader multithreading already\")\n        os.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\n    # TODO hack by collating feature_extractor and image_processor\n    feature_extractor = self.feature_extractor if self.feature_extractor is not None else self.image_processor\n    collate_fn = no_collate_fn if batch_size == 1 else pad_collate_fn(self.tokenizer, feature_extractor)\n    dataloader = DataLoader(dataset, num_workers=num_workers, batch_size=batch_size, collate_fn=collate_fn)\n    model_iterator = PipelineIterator(dataloader, self.forward, forward_params, loader_batch_size=batch_size)\n    final_iterator = PipelineIterator(model_iterator, self.postprocess, postprocess_params)\n    return final_iterator",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Pipeline.__call__` prepares batched and chat-style inputs through the following steps:\n\n*   **Chat-style Detection:** If the input is a container type (list, tuple, `GeneratorType`, or `KeyDataset`), any generator is converted to a list. The method then checks if the first element(s) are valid messages; if so, it casts the inputs into `Chat` objects (either as a single `Chat` or a list of `Chat` objects).\n*   **Parameter Merging:** It merges provided keyword arguments with internal parameters to create `preprocess_params`, `forward_params`, and `postprocess_params`.\n*   **Iterability Check:** It determines if the input is \"iterable\" (a `Dataset`, `GeneratorType`, or `list`) to determine how the data should be handled during processing.",
      "retrieval_latency_ms": 773.2868329621851,
      "generation_latency_ms": 123972.17337507755,
      "prompt_tokens": 2545,
      "completion_tokens": 1150,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9520442078728039,
      "answer_correctness": 0.7087441592288737
    },
    {
      "case_id": "transformers-t09",
      "question": "How does Pipeline.__call__ prepare batched or chat-style inputs?",
      "reference_answer": "It detects list, tuple, generator, or dataset-like inputs, converts valid message collections into Chat objects, chooses configured or default worker and batch sizes, and routes the data through a DataLoader and PipelineIterator stages.",
      "source_files": [
        "src/transformers/pipelines/base.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] get_iterator (src/transformers/pipelines/base.py)\n# [Function] Pipeline.get_iterator (src/transformers/pipelines/base.py:1169)\ndef get_iterator(\n    self, inputs, num_workers: int, batch_size: int, preprocess_params, forward_params, postprocess_params\n):\n    if isinstance(inputs, collections.abc.Sized):\n        dataset = PipelineDataset(inputs, self.preprocess, preprocess_params)\n    else:\n        if num_workers > 1:\n            logger.warning(\n                \"For iterable dataset using num_workers>1 is likely to result\"\n                \" in errors since everything is iterable, setting `num_workers=1`\"\n                \" to guarantee correctness.\"\n            )\n            num_workers = 1\n        dataset = PipelineIterator(inputs, self.preprocess, preprocess_params)\n    if \"TOKENIZERS_PARALLELISM\" not in os.environ:\n        logger.info(\"Disabling tokenizer parallelism, we're using DataLoader multithreading already\")\n        os.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\n    # TODO hack by collating feature_extractor and image_processor\n    feature_extractor = self.feature_extractor if self.feature_extractor is not None else self.image_processor\n    collate_fn = no_collate_fn if batch_size == 1 else pad_collate_fn(self.tokenizer, feature_extractor)\n    dataloader = DataLoader(dataset, num_workers=num_workers, batch_size=batch_size, collate_fn=collate_fn)\n    model_iterator = PipelineIterator(dataloader, self.forward, forward_params, loader_batch_size=batch_size)\n    final_iterator = PipelineIterator(model_iterator, self.postprocess, postprocess_params)\n    return final_iterator",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Pipeline.__call__` prepares batched and chat-style inputs through the following steps:\n\n*   **Chat-style Detection:** If the input is a container type (list, tuple, `GeneratorType`, or `KeyDataset`), any generator is converted to a list. The method then checks if the first element(s) are valid messages; if so, it casts the inputs into `Chat` objects (either as a single `Chat` or a list of `Chat` objects).\n*   **Parameter Merging:** It merges provided keyword arguments with internal parameters to create `preprocess_params`, `forward_params`, and `postprocess_params`.\n*   **Iterability Check:** It determines if the input is \"iterable\" (a `Dataset`, `GeneratorType`, or `list`) to determine how the data should be handled during processing.",
      "retrieval_latency_ms": 155.8490419993177,
      "generation_latency_ms": 99394.07433394808,
      "prompt_tokens": 2545,
      "completion_tokens": 1150,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9520442078728039,
      "answer_correctness": 0.7087441592288737
    },
    {
      "case_id": "transformers-t10",
      "question": "What does Trainer.train return and which controls are exposed at its entry point?",
      "reference_answer": "Trainer.train runs the training loop and returns a TrainOutput. Its entry point accepts an optional checkpoint to resume from, an optional hyperparameter-optimization trial, and keys to ignore during evaluation.",
      "source_files": [
        "src/transformers/trainer.py"
      ],
      "run": 1,
      "mode": "hybrid",
      "contexts": [
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):"
      ],
      "retrieved_files": [
        "src/transformers/trainer.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Trainer.train` returns a `TrainOutput` object containing the global step count, training loss, and metrics.\n\nThe controls exposed at its entry point are:\n* **resume_from_checkpoint**: A string or boolean to specify if/where to resume from a saved checkpoint.\n* **trial**: An `optuna.Trial` or dictionary for hyperparameter search.\n* **ignore_keys_for_eval**: A list of keys in the model output to ignore when gathering predictions during evaluation.",
      "retrieval_latency_ms": 660.0298329722136,
      "generation_latency_ms": 103328.87233409565,
      "prompt_tokens": 2346,
      "completion_tokens": 776,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8267508134006029,
      "answer_correctness": 0.9076627170991601
    },
    {
      "case_id": "transformers-t10",
      "question": "What does Trainer.train return and which controls are exposed at its entry point?",
      "reference_answer": "Trainer.train runs the training loop and returns a TrainOutput. Its entry point accepts an optional checkpoint to resume from, an optional hyperparameter-optimization trial, and keys to ignore during evaluation.",
      "source_files": [
        "src/transformers/trainer.py"
      ],
      "run": 1,
      "mode": "vector",
      "contexts": [
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,\n        \"return_special_tokens_mask\": return_special_tokens_mask,\n        \"return_offsets_mapping\": return_offsets_mapping,\n        \"return_length\": return_length,\n        \"split_special_tokens\": kwargs.pop(\"split_special_tokens\", self.split_special_tokens),\n        \"verbose\": verbose,\n    }\n\n    max_target_length = kwargs.pop(\"max_target_length\", None)\n\n    # First merge tokenizer_kwargs, then other kwargs (explicit params take precedence)\n    if tokenizer_kwargs is not None:\n        all_kwargs.update(tokenizer_kwargs)\n    all_kwargs.update(kwargs)\n    if text is None and text_target is None:\n        raise ValueError(\"You need to specify either `text` or `text_target`.\")\n\n    padding_strategy, truncation_strategy, max_length, kwargs = self._get_padding_truncation_strategies(\n        padding=all_kwargs.pop(\"padding\", False),\n        truncation=all_kwargs.pop(\"truncation\", None),\n        max_length=all_kwargs.pop(\"max_length\", None),\n        pad_to_multiple_of=all_kwargs.get(\"pad_to_multiple_of\"),\n        verbose=all_kwargs.get(\"verbose\", True),\n        **kwargs,\n    )\n\n    if text is not None:\n        # The context manager will send the inputs as normal texts and not text_target, but we shouldn't change the\n        # input mode in this case.\n        if not self._in_target_context_manager and hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n        encodings = self._encode_plus(\n            text=text,\n            text_pair=text_pair,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output"
      ],
      "retrieved_files": [
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py"
      ],
      "answer": "`Trainer.train` returns a `TrainOutput` object containing the global step count, training loss, and metrics.\n\nThe controls exposed at its entry point are:\n* **resume_from_checkpoint**: A `str` or `bool` to specify if/where to resume from a saved checkpoint.\n* **trial**: An `optuna.Trial` or `dict[str, Any]` for hyperparameter search.\n* **ignore_keys_for_eval**: A `list[str]` of keys in the model output to ignore when gathering predictions during evaluation.",
      "retrieval_latency_ms": 146.03887498378754,
      "generation_latency_ms": 100617.32041707728,
      "prompt_tokens": 2376,
      "completion_tokens": 698,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8267508134006029,
      "answer_correctness": 0.8481261947765433
    },
    {
      "case_id": "transformers-t01",
      "question": "What kinds of inputs can PreTrainedConfig.from_pretrained accept?",
      "reference_answer": "It accepts a model identifier hosted on Hugging Face, a directory containing a configuration saved with save_pretrained, or a path to a saved configuration JSON file. It also supports cache, download, local-only, token, and revision options.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )"
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py"
      ],
      "answer": "The `PreTrainedConfig.from_pretrained` method accepts the following types of inputs:\n\n*   **`pretrained_model_name_or_path`**: Can be a string (the model id of a pretrained model configuration hosted on huggingface.co), a path to a directory containing a configuration file, or a path to a saved configuration JSON file.\n*   **`cache_dir`**: A `str`, `os.PathLike`, or `None`.\n*   **`force_download`**: A `bool`.\n*   **`local_files_only`**: A `bool`.\n*   **`token`**: A `str`, `bool`, or `None`.\n*   **`revision`**: A `str` (defaults to \"main\").\n*   **`**kwargs`**: Arbitrary keyword arguments.",
      "retrieval_latency_ms": 142.57925003767014,
      "generation_latency_ms": 155847.07595803775,
      "prompt_tokens": 2012,
      "completion_tokens": 1305,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9576340295236717,
      "answer_correctness": 0.7175119518609271
    },
    {
      "case_id": "transformers-t01",
      "question": "What kinds of inputs can PreTrainedConfig.from_pretrained accept?",
      "reference_answer": "It accepts a model identifier hosted on Hugging Face, a directory containing a configuration saved with save_pretrained, or a path to a saved configuration JSON file. It also supports cache, download, local-only, token, and revision options.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Class] PreTrainedConfig (src/transformers/configuration_utils.py)\nGraph evidence: the Class PreTrainedConfig is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.configuration_utils.PreTrainedConfig).",
        "[Function] __call__PreTrainedConfig ()\nGraph evidence: the Function __call__PreTrainedConfig is connected by a graph relationship (node ID: __call__PreTrainedConfig).",
        "[Function] from_pretrained (src/transformers/modeling_utils.py)\nGraph evidence: the Function from_pretrained is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel.from_pretrained).",
        "[Function] from_pretrained (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Function from_pretrained is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase.from_pretrained)."
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedConfig.from_pretrained accepts the following inputs:\n\n*   **pretrained_model_name_or_path** (`str` or `os.PathLike`): Can be a model ID (string), a path to a directory containing a configuration file, or a path to a saved configuration JSON file.\n*   **cache_dir** (`str` or `os.PathLike`, optional).\n*   **force_download** (`bool`, optional).\n*   **local_files_only** (`bool`).\n*   **token** (`str` or `bool`, optional).\n*   **revision** (`str`, optional).\n*   **\\*\\*kwargs**: Additional keyword arguments.",
      "retrieval_latency_ms": 222.77883300557733,
      "generation_latency_ms": 99484.23079203349,
      "prompt_tokens": 898,
      "completion_tokens": 854,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8318701579598953,
      "answer_correctness": 0.8575209633567475
    },
    {
      "case_id": "transformers-t02",
      "question": "What does PreTrainedConfig.to_dict include when serializing a configuration?",
      "reference_answer": "It deep-copies the configuration attributes, adds the model_type when defined, records the Transformers version, and removes the unpacked kwargs entry before returning a dictionary.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)"
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "It includes a dictionary of all the attributes that make up the configuration instance, the `model_type` (if available), and the `transformers_version`. Additionally, it processes nested `PreTrainedConfig` objects by converting them to dictionaries, converts tuples to lists, and handles the `quantization_config` if it exists.",
      "retrieval_latency_ms": 103.08095789514482,
      "generation_latency_ms": 114612.34745802358,
      "prompt_tokens": 2109,
      "completion_tokens": 884,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.6487978067152046,
      "answer_correctness": 0.5133015249043251
    },
    {
      "case_id": "transformers-t02",
      "question": "What does PreTrainedConfig.to_dict include when serializing a configuration?",
      "reference_answer": "It deep-copies the configuration attributes, adds the model_type when defined, records the Transformers version, and removes the unpacked kwargs entry before returning a dictionary.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Class] PreTrainedConfig (src/transformers/configuration_utils.py)\nGraph evidence: the Class PreTrainedConfig is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.configuration_utils.PreTrainedConfig).",
        "[Function] __call__PreTrainedConfig ()\nGraph evidence: the Function __call__PreTrainedConfig is connected by a graph relationship (node ID: __call__PreTrainedConfig).",
        "[Function] to_dict (src/transformers/pipelines/base.py)\nGraph evidence: the Function to_dict is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.pipelines.base.PipelineRegistry.to_dict)."
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/pipelines/base.py"
      ],
      "answer": "PreTrainedConfig.to_dict includes:\n*   A dictionary of all attributes that make up the configuration instance (from `self.__dict__`).\n*   The `model_type` (if available on the class).\n*   The `transformers_version`.\n*   Nested configurations (converted to dictionaries).\n*   `quantization_config` (included as a dictionary if it exists and is not already a dictionary).\n\nAdditionally, the method converts tuples to lists and removes \"kwargs\".",
      "retrieval_latency_ms": 151.51129208970815,
      "generation_latency_ms": 98834.15558398701,
      "prompt_tokens": 1268,
      "completion_tokens": 809,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9097396015859399,
      "answer_correctness": 0.6019743719664987
    },
    {
      "case_id": "transformers-t03",
      "question": "How does BatchEncoding indexing behave for string, integer, and slice keys?",
      "reference_answer": "A string key returns the corresponding value from the data dictionary, an integer returns the fast-tokenizer encoding for that batch item when encodings are available, and a slice returns sliced dictionary values.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_target (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded as target texts. Each sequence can be a string or a\n            list of strings (pretokenized string). If the sequences are provided as list of strings (pretokenized),\n            you must set `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair_target (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded as target texts. Each sequence can be a string or a\n            list of strings (pretokenized string). If the sequences are provided as list of strings (pretokenized),\n            you must set `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        tokenizer_kwargs (`dict[str, Any]`, *optional*):\n            Additional kwargs to pass to the tokenizer. These will be merged with the explicit parameters and\n            other kwargs, with explicit parameters taking precedence.\n    \"\"\"\n    # To avoid duplicating\n    all_kwargs = {\n        \"add_special_tokens\": add_special_tokens,\n        \"padding\": padding,\n        \"truncation\": truncation,\n        \"max_length\": max_length,\n        \"stride\": stride,\n        \"is_split_into_words\": is_split_into_words,\n        \"pad_to_multiple_of\": pad_to_multiple_of,\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "BatchEncoding indexing behaves as follows based on the type of the key provided:\n\n*   **String:** If the key is a string, it returns the value of the dictionary associated with that key (e.g., 'input_ids', 'attention_mask').\n*   **Integer:** If the key is an integer, it returns the `tokenizers.Encoding` for the batch item at that index (provided `self._encodings` is not None).\n*   **Slice:** If the key is a slice, it returns a dictionary where each value is the data associated with the key constrained by the specified slice.",
      "retrieval_latency_ms": 95.98958294373006,
      "generation_latency_ms": 82812.86733306479,
      "prompt_tokens": 1889,
      "completion_tokens": 602,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8844242643953568,
      "answer_correctness": 0.9573010678794238
    },
    {
      "case_id": "transformers-t03",
      "question": "How does BatchEncoding indexing behave for string, integer, and slice keys?",
      "reference_answer": "A string key returns the corresponding value from the data dictionary, an integer returns the fast-tokenizer encoding for that batch item when encodings are available, and a slice returns sliced dictionary values.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Class] BatchEncoding (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class BatchEncoding is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.BatchEncoding)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "The behavior of `BatchEncoding` indexing depends on the type of the key provided:\n\n*   **String:** It returns the value of the dictionary associated with that key (e.g., 'input_ids', 'attention_mask').\n*   **Integer:** It returns the `tokenizers.Encoding` for the batch item at the specified index.\n*   **Slice:** It returns the values from the dictionary associated with that key, constrained by the slice.",
      "retrieval_latency_ms": 201.11820893362164,
      "generation_latency_ms": 71476.88183409628,
      "prompt_tokens": 1380,
      "completion_tokens": 519,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8615426189177073,
      "answer_correctness": 0.7535824615241421
    },
    {
      "case_id": "transformers-t04",
      "question": "What happens when BatchEncoding.convert_to_tensors is called with tensor_type=None?",
      "reference_answer": "It returns the BatchEncoding unchanged. When a tensor type is supplied, it converts the value to the corresponding TensorType and then performs the requested backend conversion.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "When `BatchEncoding.convert_to_tensors` is called with `tensor_type=None`, no modification is done and the method returns `self`.",
      "retrieval_latency_ms": 67.05683306790888,
      "generation_latency_ms": 60090.34958400298,
      "prompt_tokens": 2006,
      "completion_tokens": 339,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9879989514393515,
      "answer_correctness": 0.7876816686235437
    },
    {
      "case_id": "transformers-t04",
      "question": "What happens when BatchEncoding.convert_to_tensors is called with tensor_type=None?",
      "reference_answer": "It returns the BatchEncoding unchanged. When a tensor type is supplied, it converts the value to the corresponding TensorType and then performs the requested backend conversion.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Class] BatchEncoding (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class BatchEncoding is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.BatchEncoding)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "When `BatchEncoding.convert_to_tensors` is called with `tensor_type=None`, no modification is done and the method returns `self`.",
      "retrieval_latency_ms": 142.10054208524525,
      "generation_latency_ms": 48747.51370807644,
      "prompt_tokens": 1440,
      "completion_tokens": 344,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9879989514393515,
      "answer_correctness": 0.7876816686235437
    },
    {
      "case_id": "transformers-t05",
      "question": "How does PreTrainedTokenizerBase.add_tokens handle existing and special tokens?",
      "reference_answer": "It appends only tokens that are not already in the vocabulary. AddedToken objects can customize matching and whitespace behavior, and special_tokens changes normalization behavior. The method returns the number of tokens added.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedTokenizerBase.add_tokens checks if `new_tokens` is empty (returning 0 if so). If `new_tokens` is not a list or tuple, it converts it into one. It then calls the internal method `self._add_tokens(new_tokens, special_tokens=special_tokens)`.",
      "retrieval_latency_ms": 61.27987499348819,
      "generation_latency_ms": 156095.289165969,
      "prompt_tokens": 2396,
      "completion_tokens": 1242,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9382473888102069,
      "answer_correctness": 0.648996146330117
    },
    {
      "case_id": "transformers-t05",
      "question": "How does PreTrainedTokenizerBase.add_tokens handle existing and special tokens?",
      "reference_answer": "It appends only tokens that are not already in the vocabulary. AddedToken objects can customize matching and whitespace behavior, and special_tokens changes normalization behavior. The method returns the number of tokens added.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Class] PreTrainedTokenizerBase (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class PreTrainedTokenizerBase is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase).",
        "[Function] _add_tokens (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Function _add_tokens is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase._add_tokens).",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Module] tokenization_utils_base (src/transformers/tokenization_utils_base.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module tokenization_utils_base."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedTokenizerBase.add_tokens ensures that `new_tokens` is a list or tuple and then calls `self._add_tokens(new_tokens, special_tokens=special_tokens)`. The specific logic for handling existing and special tokens is handled within the `_add_tokens` method.",
      "retrieval_latency_ms": 222.0151659566909,
      "generation_latency_ms": 169748.10937501024,
      "prompt_tokens": 926,
      "completion_tokens": 1575,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9382473888102069,
      "answer_correctness": 0.5396272155720324
    },
    {
      "case_id": "transformers-t06",
      "question": "What is the main responsibility of PreTrainedTokenizerBase.__call__?",
      "reference_answer": "It is the main tokenization entry point for one or more sequences or sequence pairs. It supports padding, truncation, overflow and offset options, optional target text, and returns a BatchEncoding.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py"
      ],
      "answer": "The main responsibility of `PreTrainedTokenizerBase.__call__` is to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of sequences.",
      "retrieval_latency_ms": 185.5568749597296,
      "generation_latency_ms": 52489.85208303202,
      "prompt_tokens": 2507,
      "completion_tokens": 255,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.910347375615688,
      "answer_correctness": 0.5201651393801481
    },
    {
      "case_id": "transformers-t06",
      "question": "What is the main responsibility of PreTrainedTokenizerBase.__call__?",
      "reference_answer": "It is the main tokenization entry point for one or more sequences or sequence pairs. It supports padding, truncation, overflow and offset options, optional target text, and returns a BatchEncoding.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Class] PreTrainedTokenizerBase (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class PreTrainedTokenizerBase is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase).",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Module] tokenization_utils_base (src/transformers/tokenization_utils_base.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module tokenization_utils_base.",
        "[Class] PushToHubMixin ()\nGraph evidence: the Class PushToHubMixin is connected by a INHERITS relationship (node ID: PushToHubMixin)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "The main responsibility of `PreTrainedTokenizerBase.__call__` is to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of sequences.",
      "retrieval_latency_ms": 99.6007090434432,
      "generation_latency_ms": 38217.58137503639,
      "prompt_tokens": 1011,
      "completion_tokens": 236,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.910347375615688,
      "answer_correctness": 0.5201651393801481
    },
    {
      "case_id": "transformers-t07",
      "question": "Which options change the parameter count returned by PreTrainedModel.num_parameters?",
      "reference_answer": "only_trainable restricts the count to parameters with gradients enabled, while exclude_embeddings removes parameters belonging to embedding modules from the count.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] num_parameters (src/transformers/modeling_utils.py)\n# [Function] ModuleUtilsMixin.num_parameters (src/transformers/modeling_utils.py:1028)\ndef num_parameters(self: \"PreTrainedModel\", only_trainable: bool = False, exclude_embeddings: bool = False) -> int:\n    \"\"\"\n    Get number of (optionally, trainable or non-embeddings) parameters in the module.\n\n    Args:\n        only_trainable (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of trainable parameters\n\n        exclude_embeddings (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of non-embeddings parameters\n\n    Returns:\n        `int`: The number of parameters.\n    \"\"\"\n\n    if exclude_embeddings:\n        embedding_param_names = [\n            f\"{name}.weight\" for name, module_type in self.named_modules() if isinstance(module_type, nn.Embedding)\n        ]\n\n    is_loaded_in_4bit = getattr(self, \"is_loaded_in_4bit\", False)\n    if is_loaded_in_4bit:\n        import bitsandbytes as bnb\n\n    total_params = 0\n    for name, param in self.named_parameters():\n        if exclude_embeddings and name in embedding_param_names:\n            continue\n        if param.requires_grad or not only_trainable:\n            # For 4bit models, we need to multiply the number of parameters by 2 as half of the parameters are\n            # used for the 4bit quantization (uint8 tensors are stored)\n            if is_loaded_in_4bit and isinstance(param, bnb.nn.Params4bit):\n                if hasattr(param, \"element_size\"):\n                    num_bytes = param.element_size()\n                elif hasattr(param, \"quant_storage\"):\n                    num_bytes = param.quant_storage.itemsize\n                else:\n                    num_bytes = 1\n                total_params += param.numel() * 2 * num_bytes\n            else:\n                total_params += param.numel()\n\n    return total_params",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )"
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py"
      ],
      "answer": "The options that change the parameter count returned by `PreTrainedModel.num_parameters` are:\n\n*   **`only_trainable`**: Determines whether to return only the number of trainable parameters (defaults to `False`).\n*   **`exclude_embeddings`**: Determines whether to return only the number of non-embeddings parameters (defaults to `False`).",
      "retrieval_latency_ms": 28.85849995072931,
      "generation_latency_ms": 61629.155374947004,
      "prompt_tokens": 2395,
      "completion_tokens": 343,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9796743072542734,
      "answer_correctness": 0.7044250015186374
    },
    {
      "case_id": "transformers-t07",
      "question": "Which options change the parameter count returned by PreTrainedModel.num_parameters?",
      "reference_answer": "only_trainable restricts the count to parameters with gradients enabled, while exclude_embeddings removes parameters belonging to embedding modules from the count.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] num_parameters (src/transformers/modeling_utils.py)\n# [Function] ModuleUtilsMixin.num_parameters (src/transformers/modeling_utils.py:1028)\ndef num_parameters(self: \"PreTrainedModel\", only_trainable: bool = False, exclude_embeddings: bool = False) -> int:\n    \"\"\"\n    Get number of (optionally, trainable or non-embeddings) parameters in the module.\n\n    Args:\n        only_trainable (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of trainable parameters\n\n        exclude_embeddings (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of non-embeddings parameters\n\n    Returns:\n        `int`: The number of parameters.\n    \"\"\"\n\n    if exclude_embeddings:\n        embedding_param_names = [\n            f\"{name}.weight\" for name, module_type in self.named_modules() if isinstance(module_type, nn.Embedding)\n        ]\n\n    is_loaded_in_4bit = getattr(self, \"is_loaded_in_4bit\", False)\n    if is_loaded_in_4bit:\n        import bitsandbytes as bnb\n\n    total_params = 0\n    for name, param in self.named_parameters():\n        if exclude_embeddings and name in embedding_param_names:\n            continue\n        if param.requires_grad or not only_trainable:\n            # For 4bit models, we need to multiply the number of parameters by 2 as half of the parameters are\n            # used for the 4bit quantization (uint8 tensors are stored)\n            if is_loaded_in_4bit and isinstance(param, bnb.nn.Params4bit):\n                if hasattr(param, \"element_size\"):\n                    num_bytes = param.element_size()\n                elif hasattr(param, \"quant_storage\"):\n                    num_bytes = param.quant_storage.itemsize\n                else:\n                    num_bytes = 1\n                total_params += param.numel() * 2 * num_bytes\n            else:\n                total_params += param.numel()\n\n    return total_params",
        "[Class] PreTrainedModel (src/transformers/modeling_utils.py)\nGraph evidence: the Class PreTrainedModel is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel).",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] __call__num_parameters ()\nGraph evidence: the Function __call__num_parameters is connected by a graph relationship (node ID: __call__num_parameters).",
        "[Module] modeling_utils (src/transformers/modeling_utils.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module modeling_utils."
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py"
      ],
      "answer": "The options that change the parameter count returned by `PreTrainedModel.num_parameters` are:\n\n*   **only_trainable**: When set to `True`, it ensures only the number of trainable parameters is returned.\n*   **exclude_embeddings**: When set to `True`, it returns only the number of non-embeddings parameters.",
      "retrieval_latency_ms": 204.14287503808737,
      "generation_latency_ms": 65531.95570898242,
      "prompt_tokens": 1244,
      "completion_tokens": 489,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 0.6666666666666666,
      "answer_relevancy": 0.9933591938593486,
      "answer_correctness": 0.955539335910299
    },
    {
      "case_id": "transformers-t08",
      "question": "How does resize_token_embeddings change a pretrained model?",
      "reference_answer": "It resizes the input embedding matrix when the requested vocabulary size differs, can pad to a multiple, optionally initializes added rows using the old embedding statistics, and re-ties weights through tie_weights when the model supports it.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,\n            where the generated tokens' probabilities won't be affected by the added embeddings because initializing the new embeddings with the\n            old embeddings' mean will reduce the kl-divergence between the next token probability before and after adding the new embeddings.\n            Refer to this article for more information: https://nlp.stanford.edu/~johnhew/vocab-expansion.html\n\n    Return:\n        `torch.nn.Embedding`: Pointer to the input tokens Embeddings Module of the model.\n    \"\"\"\n    model_embeds = self._resize_token_embeddings(new_num_tokens, pad_to_multiple_of, mean_resizing)\n    if new_num_tokens is None and pad_to_multiple_of is None:\n        return model_embeds\n\n    # Since we are basically reusing the same old embeddings with new weight values, gathering is required\n    is_quantized = hasattr(self, \"hf_quantizer\") and self.hf_quantizer is not None\n    if is_deepspeed_zero3_enabled() and not is_quantized:\n        import deepspeed\n\n        with deepspeed.zero.GatheredParameters(model_embeds.weight, modifier_rank=None):\n            vocab_size = model_embeds.weight.shape[0]\n    else:\n        vocab_size = model_embeds.weight.shape[0]\n\n    # Update base model and current model config.\n    self.config.get_text_config().vocab_size = vocab_size\n    self.vocab_size = vocab_size\n\n    # Tie weights again if needed\n    self.tie_weights()\n\n    return model_embeds",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,\n        \"return_special_tokens_mask\": return_special_tokens_mask,\n        \"return_offsets_mapping\": return_offsets_mapping,\n        \"return_length\": return_length,\n        \"split_special_tokens\": kwargs.pop(\"split_special_tokens\", self.split_special_tokens),\n        \"verbose\": verbose,\n    }\n\n    max_target_length = kwargs.pop(\"max_target_length\", None)\n\n    # First merge tokenizer_kwargs, then other kwargs (explicit params take precedence)\n    if tokenizer_kwargs is not None:\n        all_kwargs.update(tokenizer_kwargs)\n    all_kwargs.update(kwargs)\n    if text is None and text_target is None:\n        raise ValueError(\"You need to specify either `text` or `text_target`.\")\n\n    padding_strategy, truncation_strategy, max_length, kwargs = self._get_padding_truncation_strategies(\n        padding=all_kwargs.pop(\"padding\", False),\n        truncation=all_kwargs.pop(\"truncation\", None),\n        max_length=all_kwargs.pop(\"max_length\", None),\n        pad_to_multiple_of=all_kwargs.get(\"pad_to_multiple_of\"),\n        verbose=all_kwargs.get(\"verbose\", True),\n        **kwargs,\n    )\n\n    if text is not None:\n        # The context manager will send the inputs as normal texts and not text_target, but we shouldn't change the\n        # input mode in this case.\n        if not self._in_target_context_manager and hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n        encodings = self._encode_plus(\n            text=text,\n            text_pair=text_pair,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)\n\n    # When fp16/bf16 full eval is enabled, __init__ skips device placement so that\n    # evaluation_loop can cast dtype and move in one step. Move the model now for training.\n    if (args.fp16_full_eval or args.bf16_full_eval) and not self.is_model_parallel and self.model_init is None:\n        self._move_model_to_device(self.model, args.device)\n\n    # Activate gradient checkpointing if needed\n    if args.gradient_checkpointing:\n        self.model.gradient_checkpointing_enable(gradient_checkpointing_kwargs=args.gradient_checkpointing_kwargs)\n\n    # If the model uses a tokenizer, it may have a new tokens for fine-tuning purposes.\n    if isinstance(self.processing_class, (PreTrainedTokenizerBase, ProcessorMixin)) and hasattr(\n        self.model, \"config\"\n    ):\n        align_special_tokens(self.model, self.processing_class)\n\n    # Attach NEFTune hooks if necessary\n    if self.neftune_noise_alpha is not None:\n        self.neftune_hook_handle = activate_neftune(self.model, self.neftune_noise_alpha, self.accelerator)\n\n    # This might change the seed so needs to run first.\n    self._hp_search_setup(trial)\n\n    if DebugOption.UNDERFLOW_OVERFLOW in args.debug:\n        if args.n_gpu > 1:\n            # nn.DataParallel(model) replicates the model, creating new variables and module\n            # references registered here no longer work on other gpus, breaking the module\n            raise ValueError(\n                \"Currently --debug underflow_overflow is not supported under DP. Please use DDP with torchrun\"\n            )\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)"
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/trainer.py"
      ],
      "answer": "`resize_token_embeddings` changes a pretrained model by:\n1. Resizing the input tokens' Embeddings Module based on `new_num_tokens`.\n2. Updating the `vocab_size` in both the model's configuration (`self.config.get_text_config().vocab_size`) and the model itself (`self.vocab_size`).\n3. Re-tying weights using `self.tie_weights()`.",
      "retrieval_latency_ms": 36.59120807424188,
      "generation_latency_ms": 109296.22870800085,
      "prompt_tokens": 2376,
      "completion_tokens": 816,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8631756891794411,
      "answer_correctness": 0.7693360399949114
    },
    {
      "case_id": "transformers-t08",
      "question": "How does resize_token_embeddings change a pretrained model?",
      "reference_answer": "It resizes the input embedding matrix when the requested vocabulary size differs, can pad to a multiple, optionally initializes added rows using the old embedding statistics, and re-ties weights through tie_weights when the model supports it.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,\n            where the generated tokens' probabilities won't be affected by the added embeddings because initializing the new embeddings with the\n            old embeddings' mean will reduce the kl-divergence between the next token probability before and after adding the new embeddings.\n            Refer to this article for more information: https://nlp.stanford.edu/~johnhew/vocab-expansion.html\n\n    Return:\n        `torch.nn.Embedding`: Pointer to the input tokens Embeddings Module of the model.\n    \"\"\"\n    model_embeds = self._resize_token_embeddings(new_num_tokens, pad_to_multiple_of, mean_resizing)\n    if new_num_tokens is None and pad_to_multiple_of is None:\n        return model_embeds\n\n    # Since we are basically reusing the same old embeddings with new weight values, gathering is required\n    is_quantized = hasattr(self, \"hf_quantizer\") and self.hf_quantizer is not None\n    if is_deepspeed_zero3_enabled() and not is_quantized:\n        import deepspeed\n\n        with deepspeed.zero.GatheredParameters(model_embeds.weight, modifier_rank=None):\n            vocab_size = model_embeds.weight.shape[0]\n    else:\n        vocab_size = model_embeds.weight.shape[0]\n\n    # Update base model and current model config.\n    self.config.get_text_config().vocab_size = vocab_size\n    self.vocab_size = vocab_size\n\n    # Tie weights again if needed\n    self.tie_weights()\n\n    return model_embeds",
        "[Function] _resize_token_embeddings (src/transformers/modeling_utils.py)\nGraph evidence: the Function _resize_token_embeddings is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel._resize_token_embeddings).",
        "[Class] PreTrainedModel (src/transformers/modeling_utils.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Class PreTrainedModel.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__hasattr ()\nGraph evidence: the Function __call__hasattr is connected by a CALLS relationship (node ID: __call__hasattr)."
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`resize_token_embeddings` changes a pretrained model by:\n1. Resizing the input tokens Embeddings Module (via `_resize_token_embeddings`).\n2. Updating the vocabulary size in both the model's configuration (`self.config.get_text_config().vocab_size`) and the model itself (`self.vocab_size`).\n3. Re-tying weights if needed using `self.tie_weights()`.",
      "retrieval_latency_ms": 212.59779192041606,
      "generation_latency_ms": 84516.99008292053,
      "prompt_tokens": 812,
      "completion_tokens": 708,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9717571695681501,
      "answer_correctness": 0.9576267846643417
    },
    {
      "case_id": "transformers-t09",
      "question": "How does Pipeline.__call__ prepare batched or chat-style inputs?",
      "reference_answer": "It detects list, tuple, generator, or dataset-like inputs, converts valid message collections into Chat objects, chooses configured or default worker and batch sizes, and routes the data through a DataLoader and PipelineIterator stages.",
      "source_files": [
        "src/transformers/pipelines/base.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] get_iterator (src/transformers/pipelines/base.py)\n# [Function] Pipeline.get_iterator (src/transformers/pipelines/base.py:1169)\ndef get_iterator(\n    self, inputs, num_workers: int, batch_size: int, preprocess_params, forward_params, postprocess_params\n):\n    if isinstance(inputs, collections.abc.Sized):\n        dataset = PipelineDataset(inputs, self.preprocess, preprocess_params)\n    else:\n        if num_workers > 1:\n            logger.warning(\n                \"For iterable dataset using num_workers>1 is likely to result\"\n                \" in errors since everything is iterable, setting `num_workers=1`\"\n                \" to guarantee correctness.\"\n            )\n            num_workers = 1\n        dataset = PipelineIterator(inputs, self.preprocess, preprocess_params)\n    if \"TOKENIZERS_PARALLELISM\" not in os.environ:\n        logger.info(\"Disabling tokenizer parallelism, we're using DataLoader multithreading already\")\n        os.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\n    # TODO hack by collating feature_extractor and image_processor\n    feature_extractor = self.feature_extractor if self.feature_extractor is not None else self.image_processor\n    collate_fn = no_collate_fn if batch_size == 1 else pad_collate_fn(self.tokenizer, feature_extractor)\n    dataloader = DataLoader(dataset, num_workers=num_workers, batch_size=batch_size, collate_fn=collate_fn)\n    model_iterator = PipelineIterator(dataloader, self.forward, forward_params, loader_batch_size=batch_size)\n    final_iterator = PipelineIterator(model_iterator, self.postprocess, postprocess_params)\n    return final_iterator",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Pipeline.__call__` prepares batched and chat-style inputs through the following steps:\n\n*   **Chat-style Detection:** If the input is a container type (list, tuple, `GeneratorType`, or `KeyDataset`), any generator is converted to a list. The method then checks if the first element(s) are valid messages; if so, it casts the inputs into `Chat` objects (either as a single `Chat` or a list of `Chat` objects).\n*   **Parameter Merging:** It merges provided keyword arguments with internal parameters to create `preprocess_params`, `forward_params`, and `postprocess_params`.\n*   **Iterability Check:** It determines if the input is \"iterable\" (a `Dataset`, `GeneratorType`, or `list`) to determine how the data should be handled during processing.",
      "retrieval_latency_ms": 100.75379197951406,
      "generation_latency_ms": 142344.28366704378,
      "prompt_tokens": 2545,
      "completion_tokens": 1150,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9520442078728039,
      "answer_correctness": 0.7087441592288737
    },
    {
      "case_id": "transformers-t09",
      "question": "How does Pipeline.__call__ prepare batched or chat-style inputs?",
      "reference_answer": "It detects list, tuple, generator, or dataset-like inputs, converts valid message collections into Chat objects, chooses configured or default worker and batch sizes, and routes the data through a DataLoader and PipelineIterator stages.",
      "source_files": [
        "src/transformers/pipelines/base.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] get_iterator (src/transformers/pipelines/base.py)\n# [Function] Pipeline.get_iterator (src/transformers/pipelines/base.py:1169)\ndef get_iterator(\n    self, inputs, num_workers: int, batch_size: int, preprocess_params, forward_params, postprocess_params\n):\n    if isinstance(inputs, collections.abc.Sized):\n        dataset = PipelineDataset(inputs, self.preprocess, preprocess_params)\n    else:\n        if num_workers > 1:\n            logger.warning(\n                \"For iterable dataset using num_workers>1 is likely to result\"\n                \" in errors since everything is iterable, setting `num_workers=1`\"\n                \" to guarantee correctness.\"\n            )\n            num_workers = 1\n        dataset = PipelineIterator(inputs, self.preprocess, preprocess_params)\n    if \"TOKENIZERS_PARALLELISM\" not in os.environ:\n        logger.info(\"Disabling tokenizer parallelism, we're using DataLoader multithreading already\")\n        os.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\n    # TODO hack by collating feature_extractor and image_processor\n    feature_extractor = self.feature_extractor if self.feature_extractor is not None else self.image_processor\n    collate_fn = no_collate_fn if batch_size == 1 else pad_collate_fn(self.tokenizer, feature_extractor)\n    dataloader = DataLoader(dataset, num_workers=num_workers, batch_size=batch_size, collate_fn=collate_fn)\n    model_iterator = PipelineIterator(dataloader, self.forward, forward_params, loader_batch_size=batch_size)\n    final_iterator = PipelineIterator(model_iterator, self.postprocess, postprocess_params)\n    return final_iterator",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Pipeline.__call__` prepares batched and chat-style inputs through the following steps:\n\n*   **Chat-style Detection:** If the input is a container type (list, tuple, `GeneratorType`, or `KeyDataset`), any generator is converted to a list. The method then checks if the first element(s) are valid messages; if so, it casts the inputs into `Chat` objects (either as a single `Chat` or a list of `Chat` objects).\n*   **Parameter Merging:** It merges provided keyword arguments with internal parameters to create `preprocess_params`, `forward_params`, and `postprocess_params`.\n*   **Iterability Check:** It determines if the input is \"iterable\" (a `Dataset`, `GeneratorType`, or `list`) to determine how the data should be handled during processing.",
      "retrieval_latency_ms": 165.04162503406405,
      "generation_latency_ms": 119097.80824999325,
      "prompt_tokens": 2545,
      "completion_tokens": 1150,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9520442078728039,
      "answer_correctness": 0.7087441592288737
    },
    {
      "case_id": "transformers-t10",
      "question": "What does Trainer.train return and which controls are exposed at its entry point?",
      "reference_answer": "Trainer.train runs the training loop and returns a TrainOutput. Its entry point accepts an optional checkpoint to resume from, an optional hyperparameter-optimization trial, and keys to ignore during evaluation.",
      "source_files": [
        "src/transformers/trainer.py"
      ],
      "run": 2,
      "mode": "vector",
      "contexts": [
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,\n        \"return_special_tokens_mask\": return_special_tokens_mask,\n        \"return_offsets_mapping\": return_offsets_mapping,\n        \"return_length\": return_length,\n        \"split_special_tokens\": kwargs.pop(\"split_special_tokens\", self.split_special_tokens),\n        \"verbose\": verbose,\n    }\n\n    max_target_length = kwargs.pop(\"max_target_length\", None)\n\n    # First merge tokenizer_kwargs, then other kwargs (explicit params take precedence)\n    if tokenizer_kwargs is not None:\n        all_kwargs.update(tokenizer_kwargs)\n    all_kwargs.update(kwargs)\n    if text is None and text_target is None:\n        raise ValueError(\"You need to specify either `text` or `text_target`.\")\n\n    padding_strategy, truncation_strategy, max_length, kwargs = self._get_padding_truncation_strategies(\n        padding=all_kwargs.pop(\"padding\", False),\n        truncation=all_kwargs.pop(\"truncation\", None),\n        max_length=all_kwargs.pop(\"max_length\", None),\n        pad_to_multiple_of=all_kwargs.get(\"pad_to_multiple_of\"),\n        verbose=all_kwargs.get(\"verbose\", True),\n        **kwargs,\n    )\n\n    if text is not None:\n        # The context manager will send the inputs as normal texts and not text_target, but we shouldn't change the\n        # input mode in this case.\n        if not self._in_target_context_manager and hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n        encodings = self._encode_plus(\n            text=text,\n            text_pair=text_pair,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output"
      ],
      "retrieved_files": [
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py"
      ],
      "answer": "`Trainer.train` returns a `TrainOutput` object containing the global step count, training loss, and metrics.\n\nThe controls exposed at its entry point are:\n* **resume_from_checkpoint**: A `str` or `bool` to specify if/where to resume from a saved checkpoint.\n* **trial**: An `optuna.Trial` or `dict[str, Any]` for hyperparameter search.\n* **ignore_keys_for_eval**: A `list[str]` of keys in the model output to ignore when gathering predictions during evaluation.",
      "retrieval_latency_ms": 63.77779203467071,
      "generation_latency_ms": 100805.32029201277,
      "prompt_tokens": 2376,
      "completion_tokens": 698,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8267508134006029,
      "answer_correctness": 0.8481261947765433
    },
    {
      "case_id": "transformers-t10",
      "question": "What does Trainer.train return and which controls are exposed at its entry point?",
      "reference_answer": "Trainer.train runs the training loop and returns a TrainOutput. Its entry point accepts an optional checkpoint to resume from, an optional hyperparameter-optimization trial, and keys to ignore during evaluation.",
      "source_files": [
        "src/transformers/trainer.py"
      ],
      "run": 2,
      "mode": "hybrid",
      "contexts": [
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):"
      ],
      "retrieved_files": [
        "src/transformers/trainer.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Trainer.train` returns a `TrainOutput` object containing the global step count, training loss, and metrics.\n\nThe controls exposed at its entry point are:\n* **resume_from_checkpoint**: A string or boolean to specify if/where to resume from a saved checkpoint.\n* **trial**: An `optuna.Trial` or dictionary for hyperparameter search.\n* **ignore_keys_for_eval**: A list of keys in the model output to ignore when gathering predictions during evaluation.",
      "retrieval_latency_ms": 185.13162503950298,
      "generation_latency_ms": 109073.68358399253,
      "prompt_tokens": 2346,
      "completion_tokens": 776,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8267508134006029,
      "answer_correctness": 0.9076627170991601
    },
    {
      "case_id": "transformers-t01",
      "question": "What kinds of inputs can PreTrainedConfig.from_pretrained accept?",
      "reference_answer": "It accepts a model identifier hosted on Hugging Face, a directory containing a configuration saved with save_pretrained, or a path to a saved configuration JSON file. It also supports cache, download, local-only, token, and revision options.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Class] PreTrainedConfig (src/transformers/configuration_utils.py)\nGraph evidence: the Class PreTrainedConfig is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.configuration_utils.PreTrainedConfig).",
        "[Function] __call__PreTrainedConfig ()\nGraph evidence: the Function __call__PreTrainedConfig is connected by a graph relationship (node ID: __call__PreTrainedConfig).",
        "[Function] from_pretrained (src/transformers/modeling_utils.py)\nGraph evidence: the Function from_pretrained is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel.from_pretrained).",
        "[Function] from_pretrained (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Function from_pretrained is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase.from_pretrained)."
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedConfig.from_pretrained accepts the following inputs:\n\n*   **pretrained_model_name_or_path** (`str` or `os.PathLike`): Can be a model ID (string), a path to a directory containing a configuration file, or a path to a saved configuration JSON file.\n*   **cache_dir** (`str` or `os.PathLike`, optional).\n*   **force_download** (`bool`, optional).\n*   **local_files_only** (`bool`).\n*   **token** (`str` or `bool`, optional).\n*   **revision** (`str`, optional).\n*   **\\*\\*kwargs**: Additional keyword arguments.",
      "retrieval_latency_ms": 239.87241589929909,
      "generation_latency_ms": 97824.81466699392,
      "prompt_tokens": 898,
      "completion_tokens": 854,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8318701579598953,
      "answer_correctness": 0.8575209633567475
    },
    {
      "case_id": "transformers-t01",
      "question": "What kinds of inputs can PreTrainedConfig.from_pretrained accept?",
      "reference_answer": "It accepts a model identifier hosted on Hugging Face, a directory containing a configuration saved with save_pretrained, or a path to a saved configuration JSON file. It also supports cache, download, local-only, token, and revision options.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )"
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py"
      ],
      "answer": "The `PreTrainedConfig.from_pretrained` method accepts the following types of inputs:\n\n*   **`pretrained_model_name_or_path`**: Can be a string (the model id of a pretrained model configuration hosted on huggingface.co), a path to a directory containing a configuration file, or a path to a saved configuration JSON file.\n*   **`cache_dir`**: A `str`, `os.PathLike`, or `None`.\n*   **`force_download`**: A `bool`.\n*   **`local_files_only`**: A `bool`.\n*   **`token`**: A `str`, `bool`, or `None`.\n*   **`revision`**: A `str` (defaults to \"main\").\n*   **`**kwargs`**: Arbitrary keyword arguments.",
      "retrieval_latency_ms": 147.61954091954976,
      "generation_latency_ms": 155308.1156249391,
      "prompt_tokens": 2012,
      "completion_tokens": 1305,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9576340295236717,
      "answer_correctness": 0.7175119518609271
    },
    {
      "case_id": "transformers-t02",
      "question": "What does PreTrainedConfig.to_dict include when serializing a configuration?",
      "reference_answer": "It deep-copies the configuration attributes, adds the model_type when defined, records the Transformers version, and removes the unpacked kwargs entry before returning a dictionary.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Class] PreTrainedConfig (src/transformers/configuration_utils.py)\nGraph evidence: the Class PreTrainedConfig is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.configuration_utils.PreTrainedConfig).",
        "[Function] __call__PreTrainedConfig ()\nGraph evidence: the Function __call__PreTrainedConfig is connected by a graph relationship (node ID: __call__PreTrainedConfig).",
        "[Function] to_dict (src/transformers/pipelines/base.py)\nGraph evidence: the Function to_dict is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.pipelines.base.PipelineRegistry.to_dict)."
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/pipelines/base.py"
      ],
      "answer": "PreTrainedConfig.to_dict includes:\n*   A dictionary of all attributes that make up the configuration instance (from `self.__dict__`).\n*   The `model_type` (if available on the class).\n*   The `transformers_version`.\n*   Nested configurations (converted to dictionaries).\n*   `quantization_config` (included as a dictionary if it exists and is not already a dictionary).\n\nAdditionally, the method converts tuples to lists and removes \"kwargs\".",
      "retrieval_latency_ms": 254.87608404364437,
      "generation_latency_ms": 99489.59666595329,
      "prompt_tokens": 1268,
      "completion_tokens": 809,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9097396015859399,
      "answer_correctness": 0.6019743719664987
    },
    {
      "case_id": "transformers-t02",
      "question": "What does PreTrainedConfig.to_dict include when serializing a configuration?",
      "reference_answer": "It deep-copies the configuration attributes, adds the model_type when defined, records the Transformers version, and removes the unpacked kwargs entry before returning a dictionary.",
      "source_files": [
        "src/transformers/configuration_utils.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)"
      ],
      "retrieved_files": [
        "src/transformers/configuration_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "It includes a dictionary of all the attributes that make up the configuration instance, the `model_type` (if available), and the `transformers_version`. Additionally, it processes nested `PreTrainedConfig` objects by converting them to dictionaries, converts tuples to lists, and handles the `quantization_config` if it exists.",
      "retrieval_latency_ms": 102.62579191476107,
      "generation_latency_ms": 104610.59358308557,
      "prompt_tokens": 2109,
      "completion_tokens": 884,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.6487978067152046,
      "answer_correctness": 0.5133015249043251
    },
    {
      "case_id": "transformers-t03",
      "question": "How does BatchEncoding indexing behave for string, integer, and slice keys?",
      "reference_answer": "A string key returns the corresponding value from the data dictionary, an integer returns the fast-tokenizer encoding for that batch item when encodings are available, and a slice returns sliced dictionary values.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Class] BatchEncoding (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class BatchEncoding is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.BatchEncoding)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "The behavior of `BatchEncoding` indexing depends on the type of the key provided:\n\n*   **String:** It returns the value of the dictionary associated with that key (e.g., 'input_ids', 'attention_mask').\n*   **Integer:** It returns the `tokenizers.Encoding` for the batch item at the specified index.\n*   **Slice:** It returns the values from the dictionary associated with that key, constrained by the slice.",
      "retrieval_latency_ms": 163.18216698709875,
      "generation_latency_ms": 71496.80691608228,
      "prompt_tokens": 1380,
      "completion_tokens": 519,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8615426189177073,
      "answer_correctness": 0.7535824615241421
    },
    {
      "case_id": "transformers-t03",
      "question": "How does BatchEncoding indexing behave for string, integer, and slice keys?",
      "reference_answer": "A string key returns the corresponding value from the data dictionary, an integer returns the fast-tokenizer encoding for that batch item when encodings are available, and a slice returns sliced dictionary values.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_target (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded as target texts. Each sequence can be a string or a\n            list of strings (pretokenized string). If the sequences are provided as list of strings (pretokenized),\n            you must set `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair_target (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded as target texts. Each sequence can be a string or a\n            list of strings (pretokenized string). If the sequences are provided as list of strings (pretokenized),\n            you must set `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        tokenizer_kwargs (`dict[str, Any]`, *optional*):\n            Additional kwargs to pass to the tokenizer. These will be merged with the explicit parameters and\n            other kwargs, with explicit parameters taking precedence.\n    \"\"\"\n    # To avoid duplicating\n    all_kwargs = {\n        \"add_special_tokens\": add_special_tokens,\n        \"padding\": padding,\n        \"truncation\": truncation,\n        \"max_length\": max_length,\n        \"stride\": stride,\n        \"is_split_into_words\": is_split_into_words,\n        \"pad_to_multiple_of\": pad_to_multiple_of,\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "BatchEncoding indexing behaves as follows based on the type of the key provided:\n\n*   **String:** If the key is a string, it returns the value of the dictionary associated with that key (e.g., 'input_ids', 'attention_mask').\n*   **Integer:** If the key is an integer, it returns the `tokenizers.Encoding` for the batch item at that index (provided `self._encodings` is not None).\n*   **Slice:** If the key is a slice, it returns a dictionary where each value is the data associated with the key constrained by the specified slice.",
      "retrieval_latency_ms": 203.3178339479491,
      "generation_latency_ms": 83946.9969170168,
      "prompt_tokens": 1889,
      "completion_tokens": 602,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8844242643953568,
      "answer_correctness": 0.9573010678794238
    },
    {
      "case_id": "transformers-t04",
      "question": "What happens when BatchEncoding.convert_to_tensors is called with tensor_type=None?",
      "reference_answer": "It returns the BatchEncoding unchanged. When a tensor type is supplied, it converts the value to the corresponding TensorType and then performs the requested backend conversion.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Class] BatchEncoding (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class BatchEncoding is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.BatchEncoding)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "When `BatchEncoding.convert_to_tensors` is called with `tensor_type=None`, no modification is done and the method returns `self`.",
      "retrieval_latency_ms": 293.03637496195734,
      "generation_latency_ms": 55318.91537492629,
      "prompt_tokens": 1440,
      "completion_tokens": 344,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9879989514393515,
      "answer_correctness": 0.7876816686235437
    },
    {
      "case_id": "transformers-t04",
      "question": "What happens when BatchEncoding.convert_to_tensors is called with tensor_type=None?",
      "reference_answer": "It returns the BatchEncoding unchanged. When a tensor type is supplied, it converts the value to the corresponding TensorType and then performs the requested backend conversion.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] __getitem__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__getitem__ (src/transformers/tokenization_utils_base.py:262)\ndef __getitem__(self, item: int | str | slice) -> _V | EncodingFast | dict[str, _V]:\n    \"\"\"\n    If the key is a string, returns the value of the dict associated to `key` ('input_ids', 'attention_mask',\n    etc.).\n\n    If the key is an integer, get the `tokenizers.Encoding` for batch item with index `key`.\n\n    If the key is a slice, returns the value of the dict associated to `key` ('input_ids', 'attention_mask', etc.)\n    with the constraint of slice.\n    \"\"\"\n    if isinstance(item, str):\n        return self.data[item]\n    elif self._encodings is not None:\n        return self._encodings[item]\n    elif isinstance(item, slice):\n        return {key: self.data[key][item] for key in self.data}\n    else:\n        raise KeyError(\n            \"Invalid key. Only three types of key are available: \"\n            \"(1) string, (2) integers for backend Encoding, and (3) slices for data subsetting.\"\n        )",
        "[Function] n_sequences (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.n_sequences (src/transformers/tokenization_utils_base.py:244)\n@property\ndef n_sequences(self) -> int | None:\n    \"\"\"\n    `Optional[int]`: The number of sequences used to generate each sample from the batch encoded in this\n    [`BatchEncoding`]. Currently can be one of `None` (unknown), `1` (a single sentence) or `2` (a pair of\n    sentences)\n    \"\"\"\n    return self._n_sequences",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "When `BatchEncoding.convert_to_tensors` is called with `tensor_type=None`, no modification is done and the method returns `self`.",
      "retrieval_latency_ms": 34.705374971963465,
      "generation_latency_ms": 42207.31441595126,
      "prompt_tokens": 2006,
      "completion_tokens": 339,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9879989514393515,
      "answer_correctness": 0.7876816686235437
    },
    {
      "case_id": "transformers-t05",
      "question": "How does PreTrainedTokenizerBase.add_tokens handle existing and special tokens?",
      "reference_answer": "It appends only tokens that are not already in the vocabulary. AddedToken objects can customize matching and whitespace behavior, and special_tokens changes normalization behavior. The method returns the number of tokens added.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Class] PreTrainedTokenizerBase (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class PreTrainedTokenizerBase is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase).",
        "[Function] _add_tokens (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Function _add_tokens is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase._add_tokens).",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Module] tokenization_utils_base (src/transformers/tokenization_utils_base.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module tokenization_utils_base."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedTokenizerBase.add_tokens ensures that `new_tokens` is a list or tuple and then calls `self._add_tokens(new_tokens, special_tokens=special_tokens)`. The specific logic for handling existing and special tokens is handled within the `_add_tokens` method.",
      "retrieval_latency_ms": 200.98870794754475,
      "generation_latency_ms": 178494.7091670474,
      "prompt_tokens": 926,
      "completion_tokens": 1575,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9382473888102069,
      "answer_correctness": 0.5396272155720324
    },
    {
      "case_id": "transformers-t05",
      "question": "How does PreTrainedTokenizerBase.add_tokens handle existing and special tokens?",
      "reference_answer": "It appends only tokens that are not already in the vocabulary. AddedToken objects can customize matching and whitespace behavior, and special_tokens changes normalization behavior. The method returns the number of tokens added.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "PreTrainedTokenizerBase.add_tokens checks if `new_tokens` is empty (returning 0 if so). If `new_tokens` is not a list or tuple, it converts it into one. It then calls the internal method `self._add_tokens(new_tokens, special_tokens=special_tokens)`.",
      "retrieval_latency_ms": 154.62129097431898,
      "generation_latency_ms": 151566.36312499177,
      "prompt_tokens": 2396,
      "completion_tokens": 1242,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9382473888102069,
      "answer_correctness": 0.648996146330117
    },
    {
      "case_id": "transformers-t06",
      "question": "What is the main responsibility of PreTrainedTokenizerBase.__call__?",
      "reference_answer": "It is the main tokenization entry point for one or more sequences or sequence pairs. It supports padding, truncation, overflow and offset options, optional target text, and returns a BatchEncoding.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Class] PreTrainedTokenizerBase (src/transformers/tokenization_utils_base.py)\nGraph evidence: the Class PreTrainedTokenizerBase is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.tokenization_utils_base.PreTrainedTokenizerBase).",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Module] tokenization_utils_base (src/transformers/tokenization_utils_base.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module tokenization_utils_base.",
        "[Class] PushToHubMixin ()\nGraph evidence: the Class PushToHubMixin is connected by a INHERITS relationship (node ID: PushToHubMixin)."
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "The main responsibility of `PreTrainedTokenizerBase.__call__` is to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of sequences.",
      "retrieval_latency_ms": 204.932292108424,
      "generation_latency_ms": 38504.664707928896,
      "prompt_tokens": 1011,
      "completion_tokens": 236,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.910347375615688,
      "answer_correctness": 0.5201651393801481
    },
    {
      "case_id": "transformers-t06",
      "question": "What is the main responsibility of PreTrainedTokenizerBase.__call__?",
      "reference_answer": "It is the main tokenization entry point for one or more sequences or sequence pairs. It supports padding, truncation, overflow and offset options, optional target text, and returns a BatchEncoding.",
      "source_files": [
        "src/transformers/tokenization_utils_base.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n@classmethod\ndef from_pretrained(\n    cls: type[SpecificPreTrainedConfigType],\n    pretrained_model_name_or_path: str | os.PathLike,\n    cache_dir: str | os.PathLike | None = None,\n    force_download: bool = False,\n    local_files_only: bool = False,\n    token: str | bool | None = None,\n    revision: str = \"main\",\n    **kwargs,\n) -> SpecificPreTrainedConfigType:\n    r\"\"\"\n    Instantiate a [`PreTrainedConfig`] (or a derived class) from a pretrained model configuration.\n\n    Args:\n        pretrained_model_name_or_path (`str` or `os.PathLike`):\n            This can be either:\n\n            - a string, the *model id* of a pretrained model configuration hosted inside a model repo on\n              huggingface.co.\n            - a path to a *directory* containing a configuration file saved using the\n              [`~PreTrainedConfig.save_pretrained`] method, e.g., `./my_model_directory/`.\n            - a path to a saved configuration JSON *file*, e.g., `./my_model_directory/configuration.json`.\n        cache_dir (`str` or `os.PathLike`, *optional*):\n            Path to a directory in which a downloaded pretrained model configuration should be cached if the\n            standard cache should not be used.\n        force_download (`bool`, *optional*, defaults to `False`):\n            Whether or not to force to (re-)download the configuration files and override the cached versions if\n            they exist.\n        proxies (`dict[str, str]`, *optional*):\n            A dictionary of proxy servers to use by protocol or endpoint, e.g., `{'http': 'foo.bar:3128',\n            'http://hostname': 'foo.bar:4012'}.` The proxies are used on each request.\n        token (`str` or `bool`, *optional*):\n            The token to use as HTTP bearer authorization for remote files. If `True`, or not specified, will use\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:"
      ],
      "retrieved_files": [
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py"
      ],
      "answer": "The main responsibility of `PreTrainedTokenizerBase.__call__` is to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of sequences.",
      "retrieval_latency_ms": 71.75724999979138,
      "generation_latency_ms": 55323.10687494464,
      "prompt_tokens": 2507,
      "completion_tokens": 255,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.910347375615688,
      "answer_correctness": 0.5201651393801481
    },
    {
      "case_id": "transformers-t07",
      "question": "Which options change the parameter count returned by PreTrainedModel.num_parameters?",
      "reference_answer": "only_trainable restricts the count to parameters with gradients enabled, while exclude_embeddings removes parameters belonging to embedding modules from the count.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] num_parameters (src/transformers/modeling_utils.py)\n# [Function] ModuleUtilsMixin.num_parameters (src/transformers/modeling_utils.py:1028)\ndef num_parameters(self: \"PreTrainedModel\", only_trainable: bool = False, exclude_embeddings: bool = False) -> int:\n    \"\"\"\n    Get number of (optionally, trainable or non-embeddings) parameters in the module.\n\n    Args:\n        only_trainable (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of trainable parameters\n\n        exclude_embeddings (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of non-embeddings parameters\n\n    Returns:\n        `int`: The number of parameters.\n    \"\"\"\n\n    if exclude_embeddings:\n        embedding_param_names = [\n            f\"{name}.weight\" for name, module_type in self.named_modules() if isinstance(module_type, nn.Embedding)\n        ]\n\n    is_loaded_in_4bit = getattr(self, \"is_loaded_in_4bit\", False)\n    if is_loaded_in_4bit:\n        import bitsandbytes as bnb\n\n    total_params = 0\n    for name, param in self.named_parameters():\n        if exclude_embeddings and name in embedding_param_names:\n            continue\n        if param.requires_grad or not only_trainable:\n            # For 4bit models, we need to multiply the number of parameters by 2 as half of the parameters are\n            # used for the 4bit quantization (uint8 tensors are stored)\n            if is_loaded_in_4bit and isinstance(param, bnb.nn.Params4bit):\n                if hasattr(param, \"element_size\"):\n                    num_bytes = param.element_size()\n                elif hasattr(param, \"quant_storage\"):\n                    num_bytes = param.quant_storage.itemsize\n                else:\n                    num_bytes = 1\n                total_params += param.numel() * 2 * num_bytes\n            else:\n                total_params += param.numel()\n\n    return total_params",
        "[Class] PreTrainedModel (src/transformers/modeling_utils.py)\nGraph evidence: the Class PreTrainedModel is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel).",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] __call__num_parameters ()\nGraph evidence: the Function __call__num_parameters is connected by a graph relationship (node ID: __call__num_parameters).",
        "[Module] modeling_utils (src/transformers/modeling_utils.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Module modeling_utils."
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py"
      ],
      "answer": "The options that change the parameter count returned by `PreTrainedModel.num_parameters` are:\n\n*   **only_trainable**: When set to `True`, it ensures only the number of trainable parameters is returned.\n*   **exclude_embeddings**: When set to `True`, it returns only the number of non-embeddings parameters.",
      "retrieval_latency_ms": 112.91145801078528,
      "generation_latency_ms": 63266.0257499665,
      "prompt_tokens": 1244,
      "completion_tokens": 489,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 0.6666666666666666,
      "answer_relevancy": 0.9933591938593486,
      "answer_correctness": 0.955539335910299
    },
    {
      "case_id": "transformers-t07",
      "question": "Which options change the parameter count returned by PreTrainedModel.num_parameters?",
      "reference_answer": "only_trainable restricts the count to parameters with gradients enabled, while exclude_embeddings removes parameters belonging to embedding modules from the count.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] num_parameters (src/transformers/modeling_utils.py)\n# [Function] ModuleUtilsMixin.num_parameters (src/transformers/modeling_utils.py:1028)\ndef num_parameters(self: \"PreTrainedModel\", only_trainable: bool = False, exclude_embeddings: bool = False) -> int:\n    \"\"\"\n    Get number of (optionally, trainable or non-embeddings) parameters in the module.\n\n    Args:\n        only_trainable (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of trainable parameters\n\n        exclude_embeddings (`bool`, *optional*, defaults to `False`):\n            Whether or not to return only the number of non-embeddings parameters\n\n    Returns:\n        `int`: The number of parameters.\n    \"\"\"\n\n    if exclude_embeddings:\n        embedding_param_names = [\n            f\"{name}.weight\" for name, module_type in self.named_modules() if isinstance(module_type, nn.Embedding)\n        ]\n\n    is_loaded_in_4bit = getattr(self, \"is_loaded_in_4bit\", False)\n    if is_loaded_in_4bit:\n        import bitsandbytes as bnb\n\n    total_params = 0\n    for name, param in self.named_parameters():\n        if exclude_embeddings and name in embedding_param_names:\n            continue\n        if param.requires_grad or not only_trainable:\n            # For 4bit models, we need to multiply the number of parameters by 2 as half of the parameters are\n            # used for the 4bit quantization (uint8 tensors are stored)\n            if is_loaded_in_4bit and isinstance(param, bnb.nn.Params4bit):\n                if hasattr(param, \"element_size\"):\n                    num_bytes = param.element_size()\n                elif hasattr(param, \"quant_storage\"):\n                    num_bytes = param.quant_storage.itemsize\n                else:\n                    num_bytes = 1\n                total_params += param.numel() * 2 * num_bytes\n            else:\n                total_params += param.numel()\n\n    return total_params",
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\ndef resize_token_embeddings(\n    self,\n    new_num_tokens: int | None = None,\n    pad_to_multiple_of: int | None = None,\n    mean_resizing: bool = True,\n) -> nn.Embedding:\n    \"\"\"\n    Resizes input token embeddings matrix of the model if `new_num_tokens != config.vocab_size`.\n\n    Takes care of tying weights embeddings afterwards if the model class has a `tie_weights()` method.\n\n    Arguments:\n        new_num_tokens (`int`, *optional*):\n            The new number of tokens in the embedding matrix. Increasing the size will add newly initialized\n            vectors at the end. Reducing the size will remove vectors from the end. If not provided or `None`, just\n            returns a pointer to the input tokens `torch.nn.Embedding` module of the model without doing anything.\n        pad_to_multiple_of (`int`, *optional*):\n            If set will pad the embedding matrix to a multiple of the provided value.If `new_num_tokens` is set to\n            `None` will just pad the embedding to a multiple of `pad_to_multiple_of`.\n\n            This is especially useful to enable the use of Tensor Cores on NVIDIA hardware with compute capability\n            `>= 7.5` (Volta), or on TPUs which benefit from having sequence lengths be a multiple of 128. For more\n            details about this, or help on choosing the correct value for resizing, refer to this guide:\n            https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html#requirements-tc\n        mean_resizing (`bool`):\n            Whether to initialize the added embeddings from a multivariate normal distribution that has old embeddings' mean and\n            covariance or to initialize them with a normal distribution that has a mean of zero and std equals `config.initializer_range`.\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()\n        target_encodings = self._encode_plus(\n            text=text_target,\n            text_pair=text_pair_target,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_target_length if max_target_length is not None else max_length,\n            **all_kwargs,\n        )\n        # Leave back tokenizer in input mode\n        if hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n\n    if text_target is None:\n        return encodings\n    elif text is None:\n        return target_encodings\n    else:\n        encodings[\"labels\"] = target_encodings[\"input_ids\"]\n        return encodings",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)\n        if resume_from_checkpoint is None:\n            raise ValueError(f\"No valid checkpoint found in output directory ({args.output_dir})\")\n\n    if resume_from_checkpoint is not None:\n        # Load model checkpoint before accelerator.prepare() for regular models,\n        # so that buffers and parameters are on the right device after prepare.\n        # Deepspeed/FSDP models are loaded after prepare in _prepare_for_training.\n        if not is_sagemaker_mp_enabled() and not self.is_deepspeed_enabled and not self.is_fsdp_enabled:\n            self._load_from_checkpoint(resume_from_checkpoint)\n        state = TrainerState.load_from_json(os.path.join(resume_from_checkpoint, TRAINER_STATE_NAME))\n        if state.train_batch_size is not None and args.auto_find_batch_size:\n            # Only restore the checkpoint's train_batch_size when using auto_find_batch_size,\n            self._train_batch_size = state.train_batch_size\n\n    inner_training_loop = find_executable_batch_size(\n        self._inner_training_loop, self._train_batch_size, args.auto_find_batch_size\n    )\n    # Disable progress bars when uploading models during checkpoints to avoid polluting stdout\n    ctx = suppress_progress_bars() if args.push_to_hub else contextlib.nullcontext()\n    with ctx:\n        return inner_training_loop(\n            args=args,\n            resume_from_checkpoint=resume_from_checkpoint,\n            trial=trial,\n            ignore_keys_for_eval=ignore_keys_for_eval,\n        )"
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/trainer.py"
      ],
      "answer": "The options that change the parameter count returned by `PreTrainedModel.num_parameters` are:\n\n*   **`only_trainable`**: Determines whether to return only the number of trainable parameters (defaults to `False`).\n*   **`exclude_embeddings`**: Determines whether to return only the number of non-embeddings parameters (defaults to `False`).",
      "retrieval_latency_ms": 157.75024995673448,
      "generation_latency_ms": 61488.98208409082,
      "prompt_tokens": 2395,
      "completion_tokens": 343,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9796743072542734,
      "answer_correctness": 0.7044250015186374
    },
    {
      "case_id": "transformers-t08",
      "question": "How does resize_token_embeddings change a pretrained model?",
      "reference_answer": "It resizes the input embedding matrix when the requested vocabulary size differs, can pad to a multiple, optionally initializes added rows using the old embedding statistics, and re-ties weights through tie_weights when the model supports it.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,\n            where the generated tokens' probabilities won't be affected by the added embeddings because initializing the new embeddings with the\n            old embeddings' mean will reduce the kl-divergence between the next token probability before and after adding the new embeddings.\n            Refer to this article for more information: https://nlp.stanford.edu/~johnhew/vocab-expansion.html\n\n    Return:\n        `torch.nn.Embedding`: Pointer to the input tokens Embeddings Module of the model.\n    \"\"\"\n    model_embeds = self._resize_token_embeddings(new_num_tokens, pad_to_multiple_of, mean_resizing)\n    if new_num_tokens is None and pad_to_multiple_of is None:\n        return model_embeds\n\n    # Since we are basically reusing the same old embeddings with new weight values, gathering is required\n    is_quantized = hasattr(self, \"hf_quantizer\") and self.hf_quantizer is not None\n    if is_deepspeed_zero3_enabled() and not is_quantized:\n        import deepspeed\n\n        with deepspeed.zero.GatheredParameters(model_embeds.weight, modifier_rank=None):\n            vocab_size = model_embeds.weight.shape[0]\n    else:\n        vocab_size = model_embeds.weight.shape[0]\n\n    # Update base model and current model config.\n    self.config.get_text_config().vocab_size = vocab_size\n    self.vocab_size = vocab_size\n\n    # Tie weights again if needed\n    self.tie_weights()\n\n    return model_embeds",
        "[Function] _resize_token_embeddings (src/transformers/modeling_utils.py)\nGraph evidence: the Function _resize_token_embeddings is connected by a graph relationship (node ID: transformers-v5.9.0-bench-20260807::transformers.modeling_utils.PreTrainedModel._resize_token_embeddings).",
        "[Class] PreTrainedModel (src/transformers/modeling_utils.py)\nGraph evidence: transformers-v5.9.0-bench-20260807 defines the Class PreTrainedModel.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__hasattr ()\nGraph evidence: the Function __call__hasattr is connected by a CALLS relationship (node ID: __call__hasattr)."
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`resize_token_embeddings` changes a pretrained model by:\n1. Resizing the input tokens Embeddings Module (via `_resize_token_embeddings`).\n2. Updating the vocabulary size in both the model's configuration (`self.config.get_text_config().vocab_size`) and the model itself (`self.vocab_size`).\n3. Re-tying weights if needed using `self.tie_weights()`.",
      "retrieval_latency_ms": 106.83804203290492,
      "generation_latency_ms": 82534.2425419949,
      "prompt_tokens": 812,
      "completion_tokens": 708,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9717571695681501,
      "answer_correctness": 0.9576267846643417
    },
    {
      "case_id": "transformers-t08",
      "question": "How does resize_token_embeddings change a pretrained model?",
      "reference_answer": "It resizes the input embedding matrix when the requested vocabulary size differs, can pad to a multiple, optionally initializes added rows using the old embedding statistics, and re-ties weights through tie_weights when the model supports it.",
      "source_files": [
        "src/transformers/modeling_utils.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] resize_token_embeddings (src/transformers/modeling_utils.py)\n# [Function] PreTrainedModel.resize_token_embeddings (src/transformers/modeling_utils.py:2728)\n\n            Setting `mean_resizing` to `True` is useful when increasing the size of the embeddings of causal language models,\n            where the generated tokens' probabilities won't be affected by the added embeddings because initializing the new embeddings with the\n            old embeddings' mean will reduce the kl-divergence between the next token probability before and after adding the new embeddings.\n            Refer to this article for more information: https://nlp.stanford.edu/~johnhew/vocab-expansion.html\n\n    Return:\n        `torch.nn.Embedding`: Pointer to the input tokens Embeddings Module of the model.\n    \"\"\"\n    model_embeds = self._resize_token_embeddings(new_num_tokens, pad_to_multiple_of, mean_resizing)\n    if new_num_tokens is None and pad_to_multiple_of is None:\n        return model_embeds\n\n    # Since we are basically reusing the same old embeddings with new weight values, gathering is required\n    is_quantized = hasattr(self, \"hf_quantizer\") and self.hf_quantizer is not None\n    if is_deepspeed_zero3_enabled() and not is_quantized:\n        import deepspeed\n\n        with deepspeed.zero.GatheredParameters(model_embeds.weight, modifier_rank=None):\n            vocab_size = model_embeds.weight.shape[0]\n    else:\n        vocab_size = model_embeds.weight.shape[0]\n\n    # Update base model and current model config.\n    self.config.get_text_config().vocab_size = vocab_size\n    self.vocab_size = vocab_size\n\n    # Tie weights again if needed\n    self.tie_weights()\n\n    return model_embeds",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,\n        \"return_special_tokens_mask\": return_special_tokens_mask,\n        \"return_offsets_mapping\": return_offsets_mapping,\n        \"return_length\": return_length,\n        \"split_special_tokens\": kwargs.pop(\"split_special_tokens\", self.split_special_tokens),\n        \"verbose\": verbose,\n    }\n\n    max_target_length = kwargs.pop(\"max_target_length\", None)\n\n    # First merge tokenizer_kwargs, then other kwargs (explicit params take precedence)\n    if tokenizer_kwargs is not None:\n        all_kwargs.update(tokenizer_kwargs)\n    all_kwargs.update(kwargs)\n    if text is None and text_target is None:\n        raise ValueError(\"You need to specify either `text` or `text_target`.\")\n\n    padding_strategy, truncation_strategy, max_length, kwargs = self._get_padding_truncation_strategies(\n        padding=all_kwargs.pop(\"padding\", False),\n        truncation=all_kwargs.pop(\"truncation\", None),\n        max_length=all_kwargs.pop(\"max_length\", None),\n        pad_to_multiple_of=all_kwargs.get(\"pad_to_multiple_of\"),\n        verbose=all_kwargs.get(\"verbose\", True),\n        **kwargs,\n    )\n\n    if text is not None:\n        # The context manager will send the inputs as normal texts and not text_target, but we shouldn't change the\n        # input mode in this case.\n        if not self._in_target_context_manager and hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n        encodings = self._encode_plus(\n            text=text,\n            text_pair=text_pair,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)\n\n    # When fp16/bf16 full eval is enabled, __init__ skips device placement so that\n    # evaluation_loop can cast dtype and move in one step. Move the model now for training.\n    if (args.fp16_full_eval or args.bf16_full_eval) and not self.is_model_parallel and self.model_init is None:\n        self._move_model_to_device(self.model, args.device)\n\n    # Activate gradient checkpointing if needed\n    if args.gradient_checkpointing:\n        self.model.gradient_checkpointing_enable(gradient_checkpointing_kwargs=args.gradient_checkpointing_kwargs)\n\n    # If the model uses a tokenizer, it may have a new tokens for fine-tuning purposes.\n    if isinstance(self.processing_class, (PreTrainedTokenizerBase, ProcessorMixin)) and hasattr(\n        self.model, \"config\"\n    ):\n        align_special_tokens(self.model, self.processing_class)\n\n    # Attach NEFTune hooks if necessary\n    if self.neftune_noise_alpha is not None:\n        self.neftune_hook_handle = activate_neftune(self.model, self.neftune_noise_alpha, self.accelerator)\n\n    # This might change the seed so needs to run first.\n    self._hp_search_setup(trial)\n\n    if DebugOption.UNDERFLOW_OVERFLOW in args.debug:\n        if args.n_gpu > 1:\n            # nn.DataParallel(model) replicates the model, creating new variables and module\n            # references registered here no longer work on other gpus, breaking the module\n            raise ValueError(\n                \"Currently --debug underflow_overflow is not supported under DP. Please use DDP with torchrun\"\n            )\n        else:\n            DebugUnderflowOverflow(self.model)\n\n    # Load potential model checkpoint\n    if isinstance(resume_from_checkpoint, bool) and resume_from_checkpoint:\n        resume_from_checkpoint = get_last_checkpoint(args.output_dir)"
      ],
      "retrieved_files": [
        "src/transformers/modeling_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/trainer.py"
      ],
      "answer": "`resize_token_embeddings` changes a pretrained model by:\n1. Resizing the input tokens' Embeddings Module based on `new_num_tokens`.\n2. Updating the `vocab_size` in both the model's configuration (`self.config.get_text_config().vocab_size`) and the model itself (`self.vocab_size`).\n3. Re-tying weights using `self.tie_weights()`.",
      "retrieval_latency_ms": 150.34425002522767,
      "generation_latency_ms": 107138.16991704516,
      "prompt_tokens": 2376,
      "completion_tokens": 816,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8631756891794411,
      "answer_correctness": 0.7693360399949114
    },
    {
      "case_id": "transformers-t09",
      "question": "How does Pipeline.__call__ prepare batched or chat-style inputs?",
      "reference_answer": "It detects list, tuple, generator, or dataset-like inputs, converts valid message collections into Chat objects, chooses configured or default worker and batch sizes, and routes the data through a DataLoader and PipelineIterator stages.",
      "source_files": [
        "src/transformers/pipelines/base.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] get_iterator (src/transformers/pipelines/base.py)\n# [Function] Pipeline.get_iterator (src/transformers/pipelines/base.py:1169)\ndef get_iterator(\n    self, inputs, num_workers: int, batch_size: int, preprocess_params, forward_params, postprocess_params\n):\n    if isinstance(inputs, collections.abc.Sized):\n        dataset = PipelineDataset(inputs, self.preprocess, preprocess_params)\n    else:\n        if num_workers > 1:\n            logger.warning(\n                \"For iterable dataset using num_workers>1 is likely to result\"\n                \" in errors since everything is iterable, setting `num_workers=1`\"\n                \" to guarantee correctness.\"\n            )\n            num_workers = 1\n        dataset = PipelineIterator(inputs, self.preprocess, preprocess_params)\n    if \"TOKENIZERS_PARALLELISM\" not in os.environ:\n        logger.info(\"Disabling tokenizer parallelism, we're using DataLoader multithreading already\")\n        os.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\n    # TODO hack by collating feature_extractor and image_processor\n    feature_extractor = self.feature_extractor if self.feature_extractor is not None else self.image_processor\n    collate_fn = no_collate_fn if batch_size == 1 else pad_collate_fn(self.tokenizer, feature_extractor)\n    dataloader = DataLoader(dataset, num_workers=num_workers, batch_size=batch_size, collate_fn=collate_fn)\n    model_iterator = PipelineIterator(dataloader, self.forward, forward_params, loader_batch_size=batch_size)\n    final_iterator = PipelineIterator(model_iterator, self.postprocess, postprocess_params)\n    return final_iterator",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Pipeline.__call__` prepares batched and chat-style inputs through the following steps:\n\n*   **Chat-style Detection:** If the input is a container type (list, tuple, `GeneratorType`, or `KeyDataset`), any generator is converted to a list. The method then checks if the first element(s) are valid messages; if so, it casts the inputs into `Chat` objects (either as a single `Chat` or a list of `Chat` objects).\n*   **Parameter Merging:** It merges provided keyword arguments with internal parameters to create `preprocess_params`, `forward_params`, and `postprocess_params`.\n*   **Iterability Check:** It determines if the input is \"iterable\" (a `Dataset`, `GeneratorType`, or `list`) to determine how the data should be handled during processing.",
      "retrieval_latency_ms": 58.6144580738619,
      "generation_latency_ms": 145483.5933750728,
      "prompt_tokens": 2545,
      "completion_tokens": 1150,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9520442078728039,
      "answer_correctness": 0.7087441592288737
    },
    {
      "case_id": "transformers-t09",
      "question": "How does Pipeline.__call__ prepare batched or chat-style inputs?",
      "reference_answer": "It detects list, tuple, generator, or dataset-like inputs, converts valid message collections into Chat objects, chooses configured or default worker and batch sizes, and routes the data through a DataLoader and PipelineIterator stages.",
      "source_files": [
        "src/transformers/pipelines/base.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] __call__ (src/transformers/pipelines/base.py)\n# [Function] Pipeline.__call__ (src/transformers/pipelines/base.py:1194)\ndef __call__(self, inputs, *args, num_workers=None, batch_size=None, **kwargs):\n    if args:\n        logger.warning(f\"Ignoring args : {args}\")\n\n    # Detect if inputs are a chat-style input(s) and cast as `Chat` or list of `Chat`\n    container_types = (list, tuple, types.GeneratorType)\n    if is_torch_available():\n        container_types = (*container_types, KeyDataset)\n    if isinstance(inputs, container_types):\n        if isinstance(inputs, types.GeneratorType):\n            inputs = list(inputs)\n        if is_valid_message(inputs[0]):\n            inputs = Chat(inputs)\n        elif isinstance(inputs[0], (list, tuple)) and all(chat and is_valid_message(chat[0]) for chat in inputs):\n            inputs = [Chat(chat) for chat in inputs]\n\n    if num_workers is None:\n        if self._num_workers is None:\n            num_workers = 0\n        else:\n            num_workers = self._num_workers\n    if batch_size is None:\n        if self._batch_size is None:\n            batch_size = 1\n        else:\n            batch_size = self._batch_size\n\n    preprocess_params, forward_params, postprocess_params = self._sanitize_parameters(**kwargs)\n\n    # Fuse __init__ params and __call__ params without modifying the __init__ ones.\n    preprocess_params = {**self._preprocess_params, **preprocess_params}\n    forward_params = {**self._forward_params, **forward_params}\n    postprocess_params = {**self._postprocess_params, **postprocess_params}\n\n    self.call_count += 1\n    if self.call_count > 10 and self.device.type == \"cuda\":\n        logger.warning_once(\n            \"You seem to be using the pipelines sequentially on GPU. In order to maximize efficiency please use a\"\n            \" dataset\",\n        )\n\n    is_dataset = Dataset is not None and isinstance(inputs, Dataset)\n    is_generator = isinstance(inputs, types.GeneratorType)\n    is_list = isinstance(inputs, list)\n\n    is_iterable = is_dataset or is_generator or is_list\n    can_use_iterator = is_dataset or is_generator or is_list\n\n    if is_list:\n        if can_use_iterator:",
        "[Function] __init__ (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.__init__ (src/transformers/tokenization_utils_base.py:221)\ndef __init__(\n    self,\n    data: dict[str, Any] | None = None,\n    encoding: EncodingFast | Sequence[EncodingFast] | None = None,\n    tensor_type: None | str | TensorType = None,\n    prepend_batch_axis: bool = False,\n    n_sequences: int | None = None,\n):\n    super().__init__(data)\n\n    # If encoding is not None, the fast tokenization is used\n    if encoding is not None and isinstance(encoding, EncodingFast):\n        encoding = [encoding]\n\n    self._encodings = encoding\n\n    if n_sequences is None and encoding is not None and encoding:\n        n_sequences = encoding[0].n_sequences\n\n    self._n_sequences = n_sequences\n\n    self.convert_to_tensors(tensor_type=tensor_type, prepend_batch_axis=prepend_batch_axis)",
        "[Function] get_iterator (src/transformers/pipelines/base.py)\n# [Function] Pipeline.get_iterator (src/transformers/pipelines/base.py:1169)\ndef get_iterator(\n    self, inputs, num_workers: int, batch_size: int, preprocess_params, forward_params, postprocess_params\n):\n    if isinstance(inputs, collections.abc.Sized):\n        dataset = PipelineDataset(inputs, self.preprocess, preprocess_params)\n    else:\n        if num_workers > 1:\n            logger.warning(\n                \"For iterable dataset using num_workers>1 is likely to result\"\n                \" in errors since everything is iterable, setting `num_workers=1`\"\n                \" to guarantee correctness.\"\n            )\n            num_workers = 1\n        dataset = PipelineIterator(inputs, self.preprocess, preprocess_params)\n    if \"TOKENIZERS_PARALLELISM\" not in os.environ:\n        logger.info(\"Disabling tokenizer parallelism, we're using DataLoader multithreading already\")\n        os.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\n    # TODO hack by collating feature_extractor and image_processor\n    feature_extractor = self.feature_extractor if self.feature_extractor is not None else self.image_processor\n    collate_fn = no_collate_fn if batch_size == 1 else pad_collate_fn(self.tokenizer, feature_extractor)\n    dataloader = DataLoader(dataset, num_workers=num_workers, batch_size=batch_size, collate_fn=collate_fn)\n    model_iterator = PipelineIterator(dataloader, self.forward, forward_params, loader_batch_size=batch_size)\n    final_iterator = PipelineIterator(model_iterator, self.postprocess, postprocess_params)\n    return final_iterator",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):\n                value_lens = [len(val) for val in value]\n                if len(set(value_lens)) > 1 and dtype is None:\n                    # we have a ragged list so handle explicitly\n                    value = as_tensor([np.asarray(val) for val in value], dtype=object)\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = np.int64\n            return np.asarray(value, dtype=dtype)\n\n        is_tensor = is_numpy_array\n\n    # Do the tensor conversion in batch\n    for key, value in self.items():\n        try:\n            if prepend_batch_axis:\n                value = [value]\n\n            if not is_tensor(value):\n                tensor = as_tensor(value)\n\n                # Removing this for now in favor of controlling the shape with `prepend_batch_axis`\n                # # at-least2d\n                # if tensor.ndim > 2:\n                #     tensor = tensor.squeeze(0)\n                # elif tensor.ndim < 2:\n                #     tensor = tensor[None, :]\n\n                self[key] = tensor\n        except Exception as e:\n            if key == \"overflowing_tokens\":\n                raise ValueError(\n                    \"Unable to create tensor returning overflowing tokens of different lengths. \"\n                    \"Please see if a fast version of this tokenizer is available to have this feature available.\"\n                ) from e\n            raise ValueError(\n                \"Unable to create tensor, you should probably activate truncation and/or padding with\"\n                \" 'padding=True' 'truncation=True' to have batched tensors with the same length. Perhaps your\"\n                f\" features (`{key}` in this case) have excessive nesting (inputs type `list` where type `int` is\"\n                \" expected).\"\n            ) from e\n\n    return self",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n@add_end_docstrings(ENCODE_KWARGS_DOCSTRING, ENCODE_PLUS_ADDITIONAL_KWARGS_DOCSTRING)\ndef __call__(\n    self,\n    text: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    text_pair_target: TextInput | PreTokenizedInput | list[TextInput] | list[PreTokenizedInput] | None = None,\n    add_special_tokens: bool = True,\n    padding: bool | str | PaddingStrategy = False,\n    truncation: bool | str | TruncationStrategy | None = None,\n    max_length: int | None = None,\n    stride: int = 0,\n    is_split_into_words: bool = False,\n    pad_to_multiple_of: int | None = None,\n    padding_side: str | None = None,\n    return_tensors: str | TensorType | None = None,\n    return_token_type_ids: bool | None = None,\n    return_attention_mask: bool | None = None,\n    return_overflowing_tokens: bool = False,\n    return_special_tokens_mask: bool = False,\n    return_offsets_mapping: bool = False,\n    return_length: bool = False,\n    verbose: bool = True,\n    tokenizer_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -> BatchEncoding:\n    \"\"\"\n    Main method to tokenize and prepare for the model one or several sequence(s) or one or several pair(s) of\n    sequences.\n\n    Args:\n        text (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings\n            (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set\n            `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).\n        text_pair (`str`, `list[str]`, `list[list[str]]`, *optional*):\n            The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings"
      ],
      "retrieved_files": [
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/pipelines/base.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Pipeline.__call__` prepares batched and chat-style inputs through the following steps:\n\n*   **Chat-style Detection:** If the input is a container type (list, tuple, `GeneratorType`, or `KeyDataset`), any generator is converted to a list. The method then checks if the first element(s) are valid messages; if so, it casts the inputs into `Chat` objects (either as a single `Chat` or a list of `Chat` objects).\n*   **Parameter Merging:** It merges provided keyword arguments with internal parameters to create `preprocess_params`, `forward_params`, and `postprocess_params`.\n*   **Iterability Check:** It determines if the input is \"iterable\" (a `Dataset`, `GeneratorType`, or `list`) to determine how the data should be handled during processing.",
      "retrieval_latency_ms": 156.45579190459102,
      "generation_latency_ms": 119057.50929098576,
      "prompt_tokens": 2545,
      "completion_tokens": 1150,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.9520442078728039,
      "answer_correctness": 0.7087441592288737
    },
    {
      "case_id": "transformers-t10",
      "question": "What does Trainer.train return and which controls are exposed at its entry point?",
      "reference_answer": "Trainer.train runs the training loop and returns a TrainOutput. Its entry point accepts an optional checkpoint to resume from, an optional hyperparameter-optimization trial, and keys to ignore during evaluation.",
      "source_files": [
        "src/transformers/trainer.py"
      ],
      "run": 3,
      "mode": "hybrid",
      "contexts": [
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output",
        "[Function] convert_to_tensors (src/transformers/tokenization_utils_base.py)\n# [Function] BatchEncoding.convert_to_tensors (src/transformers/tokenization_utils_base.py:673)\ndef convert_to_tensors(self, tensor_type: str | TensorType | None = None, prepend_batch_axis: bool = False):\n    \"\"\"\n    Convert the inner content to tensors.\n\n    Args:\n        tensor_type (`str` or [`~utils.TensorType`], *optional*):\n            The type of tensors to use. If `str`, should be one of the values of the enum [`~utils.TensorType`]. If\n            `None`, no modification is done.\n        prepend_batch_axis (`int`, *optional*, defaults to `False`):\n            Whether or not to add the batch dimension during the conversion.\n    \"\"\"\n    if tensor_type is None:\n        return self\n\n    # Convert to TensorType\n    if not isinstance(tensor_type, TensorType):\n        tensor_type = TensorType(tensor_type)\n\n    if tensor_type == TensorType.PYTORCH:\n        if not is_torch_available():\n            raise ImportError(\"Unable to convert output to PyTorch tensors format, PyTorch is not installed.\")\n        import torch\n\n        def as_tensor(value, dtype=None):\n            if isinstance(value, list) and len(value) > 0 and isinstance(value[0], np.ndarray):\n                return torch.from_numpy(np.array(value))\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = torch.int64\n            return torch.tensor(value, dtype=dtype)\n\n        is_tensor = torch.is_tensor\n\n    elif tensor_type == TensorType.MLX:\n        if not is_mlx_available():\n            raise ImportError(\"Unable to convert output to MLX tensors format, MLX is not installed.\")\n        import mlx.core as mx\n\n        def as_tensor(value, dtype=None):\n            if len(flatten(value)) == 0 and dtype is None:\n                dtype = mx.int32\n            return mx.array(value, dtype=dtype)\n\n        def is_tensor(obj):\n            return isinstance(obj, mx.array)\n    else:\n\n        def as_tensor(value, dtype=None):\n            if (\n                isinstance(value, (list, tuple))\n                and len(value) > 0\n                and isinstance(value[0], (list, tuple, np.ndarray))\n            ):"
      ],
      "retrieved_files": [
        "src/transformers/trainer.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py"
      ],
      "answer": "`Trainer.train` returns a `TrainOutput` object containing the global step count, training loss, and metrics.\n\nThe controls exposed at its entry point are:\n* **resume_from_checkpoint**: A string or boolean to specify if/where to resume from a saved checkpoint.\n* **trial**: An `optuna.Trial` or dictionary for hyperparameter search.\n* **ignore_keys_for_eval**: A list of keys in the model output to ignore when gathering predictions during evaluation.",
      "retrieval_latency_ms": 147.4326669704169,
      "generation_latency_ms": 107806.68529192917,
      "prompt_tokens": 2346,
      "completion_tokens": 776,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8267508134006029,
      "answer_correctness": 0.9076627170991601
    },
    {
      "case_id": "transformers-t10",
      "question": "What does Trainer.train return and which controls are exposed at its entry point?",
      "reference_answer": "Trainer.train runs the training loop and returns a TrainOutput. Its entry point accepts an optional checkpoint to resume from, an optional hyperparameter-optimization trial, and keys to ignore during evaluation.",
      "source_files": [
        "src/transformers/trainer.py"
      ],
      "run": 3,
      "mode": "vector",
      "contexts": [
        "[Function] train (src/transformers/trainer.py)\n# [Function] Trainer.train (src/transformers/trainer.py:1325)\ndef train(\n    self,\n    resume_from_checkpoint: str | bool | None = None,\n    trial: \"optuna.Trial | dict[str, Any] | None\" = None,\n    ignore_keys_for_eval: list[str] | None = None,\n) -> TrainOutput:\n    \"\"\"\n    Main training entry point.\n\n    Args:\n        resume_from_checkpoint (`str` or `bool`, *optional*):\n            If a `str`, local path to a saved checkpoint as saved by a previous instance of [`Trainer`]. If a\n            `bool` and equals `True`, load the last checkpoint in *args.output_dir* as saved by a previous instance\n            of [`Trainer`]. If present, training will resume from the model/optimizer/scheduler states loaded here.\n        trial (`optuna.Trial` or `dict[str, Any]`, *optional*):\n            The trial run or the hyperparameter dictionary for hyperparameter search.\n        ignore_keys_for_eval (`list[str]`, *optional*)\n            A list of keys in the output of your model (if it is a dictionary) that should be ignored when\n            gathering predictions for evaluation during the training.\n\n    Returns:\n        [`~trainer_utils.TrainOutput`]: Object containing the global step count, training loss, and metrics.\n    \"\"\"\n    if resume_from_checkpoint is False:\n        resume_from_checkpoint = None\n\n    # memory metrics - must set up as early as possible\n    self._memory_tracker.start()\n\n    args = self.args\n\n    self.is_in_train = True\n\n    # Model re-init\n    if self.model_init is not None:\n        # Seed must be set before instantiating the model when using model_init.\n        enable_full_determinism(args.seed) if args.full_determinism else set_seed(args.seed)\n        self.model = self.call_model_init(trial)\n        # Reinitializes optimizer and scheduler\n        self.optimizer, self.lr_scheduler = None, None\n        if self.place_model_on_device:\n            self._move_model_to_device(self.model, args.device)\n        self.model_wrapped = self.model\n\n    if self.args.use_liger_kernel:\n        apply_liger_kernel(self.model, self.args.liger_kernel_config)",
        "[Function] __call__ (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.__call__ (src/transformers/tokenization_utils_base.py:2418)\n        \"padding_side\": padding_side,\n        \"return_tensors\": return_tensors,\n        \"return_token_type_ids\": return_token_type_ids,\n        \"return_attention_mask\": return_attention_mask,\n        \"return_overflowing_tokens\": return_overflowing_tokens,\n        \"return_special_tokens_mask\": return_special_tokens_mask,\n        \"return_offsets_mapping\": return_offsets_mapping,\n        \"return_length\": return_length,\n        \"split_special_tokens\": kwargs.pop(\"split_special_tokens\", self.split_special_tokens),\n        \"verbose\": verbose,\n    }\n\n    max_target_length = kwargs.pop(\"max_target_length\", None)\n\n    # First merge tokenizer_kwargs, then other kwargs (explicit params take precedence)\n    if tokenizer_kwargs is not None:\n        all_kwargs.update(tokenizer_kwargs)\n    all_kwargs.update(kwargs)\n    if text is None and text_target is None:\n        raise ValueError(\"You need to specify either `text` or `text_target`.\")\n\n    padding_strategy, truncation_strategy, max_length, kwargs = self._get_padding_truncation_strategies(\n        padding=all_kwargs.pop(\"padding\", False),\n        truncation=all_kwargs.pop(\"truncation\", None),\n        max_length=all_kwargs.pop(\"max_length\", None),\n        pad_to_multiple_of=all_kwargs.get(\"pad_to_multiple_of\"),\n        verbose=all_kwargs.get(\"verbose\", True),\n        **kwargs,\n    )\n\n    if text is not None:\n        # The context manager will send the inputs as normal texts and not text_target, but we shouldn't change the\n        # input mode in this case.\n        if not self._in_target_context_manager and hasattr(self, \"_switch_to_input_mode\"):\n            self._switch_to_input_mode()\n        encodings = self._encode_plus(\n            text=text,\n            text_pair=text_pair,\n            padding_strategy=padding_strategy,\n            truncation_strategy=truncation_strategy,\n            max_length=max_length,\n            **all_kwargs,\n        )\n    if text_target is not None:\n        if hasattr(self, \"_switch_to_target_mode\"):\n            self._switch_to_target_mode()",
        "[Function] from_pretrained (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.from_pretrained (src/transformers/configuration_utils.py:554)\n            the token generated when running `hf auth login` (stored in `~/.huggingface`).\n        revision (`str`, *optional*, defaults to `\"main\"`):\n            The specific model version to use. It can be a branch name, a tag name, or a commit id, since we use a\n            git-based system for storing models and other artifacts on huggingface.co, so `revision` can be any\n            identifier allowed by git.\n\n            <Tip>\n\n            To test a pull request you made on the Hub, you can pass `revision=\"refs/pr/<pr_number>\"`.\n\n            </Tip>\n\n        return_unused_kwargs (`bool`, *optional*, defaults to `False`):\n            If `False`, then this function returns just the final configuration object.\n\n            If `True`, then this functions returns a `Tuple(config, unused_kwargs)` where *unused_kwargs* is a\n            dictionary consisting of the key/value pairs whose keys are not configuration attributes: i.e., the\n            part of `kwargs` which has not been used to update `config` and is otherwise ignored.\n        subfolder (`str`, *optional*, defaults to `\"\"`):\n            In case the relevant files are located inside a subfolder of the model repo on huggingface.co, you can\n            specify the folder name here.\n        kwargs (`dict[str, Any]`, *optional*):\n            The values in kwargs of any keys which are configuration attributes will be used to override the loaded\n            values. Behavior concerning key/value pairs whose keys are *not* configuration attributes is controlled\n            by the `return_unused_kwargs` keyword parameter.\n\n    Returns:\n        [`PreTrainedConfig`]: The configuration object instantiated from this pretrained model.\n\n    Examples:\n\n    ```python\n    # We can't instantiate directly the base class *PreTrainedConfig* so let's show the examples on a\n    # derived class: BertConfig\n    config = BertConfig.from_pretrained(\n        \"google-bert/bert-base-uncased\"\n    )  # Download configuration from huggingface.co and cache.",
        "[Function] add_tokens (src/transformers/tokenization_utils_base.py)\n# [Function] PreTrainedTokenizerBase.add_tokens (src/transformers/tokenization_utils_base.py:1216)\n    model.resize_token_embeddings(len(tokenizer))\n    ```\"\"\"\n    if not new_tokens:\n        return 0\n\n    if not isinstance(new_tokens, (list, tuple)):\n        new_tokens = [new_tokens]\n    return self._add_tokens(new_tokens, special_tokens=special_tokens)",
        "[Function] to_dict (src/transformers/configuration_utils.py)\n# [Function] PreTrainedConfig.to_dict (src/transformers/configuration_utils.py:1004)\ndef to_dict(self) -> dict[str, Any]:\n    \"\"\"\n    Serializes this instance to a Python dictionary.\n\n    Returns:\n        `dict[str, Any]`: Dictionary of all the attributes that make up this configuration instance.\n    \"\"\"\n    output = copy.deepcopy(self.__dict__)\n    if hasattr(self.__class__, \"model_type\"):\n        output[\"model_type\"] = self.__class__.model_type\n\n    # Transformers version when serializing the model\n    output[\"transformers_version\"] = __version__\n\n    # Pop \"kwargs\" since they are unpacked and set in the post init\n    output.pop(\"kwargs\", None)\n\n    def to_list(value):\n        if isinstance(value, tuple):\n            value = [to_list(item) for item in value]\n        return value\n\n    for key, value in output.items():\n        # Deal with nested configs like CLIP\n        if isinstance(value, PreTrainedConfig):\n            value = value.to_dict()\n            del value[\"transformers_version\"]\n\n        # Some models have defaults as tuples because dataclass\n        # doesn't allow mutables. Let's convert back to `list``\n        elif isinstance(value, tuple):\n            value = to_list(value)\n\n        output[key] = value\n\n    self._remove_keys_not_serialized(output)\n\n    if hasattr(self, \"quantization_config\"):\n        output[\"quantization_config\"] = (\n            self.quantization_config.to_dict()\n            if not isinstance(self.quantization_config, dict) and self.quantization_config is not None\n            else self.quantization_config\n        )\n    self.dict_dtype_to_str(output)\n\n    return output"
      ],
      "retrieved_files": [
        "src/transformers/trainer.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py",
        "src/transformers/tokenization_utils_base.py",
        "src/transformers/configuration_utils.py"
      ],
      "answer": "`Trainer.train` returns a `TrainOutput` object containing the global step count, training loss, and metrics.\n\nThe controls exposed at its entry point are:\n* **resume_from_checkpoint**: A `str` or `bool` to specify if/where to resume from a saved checkpoint.\n* **trial**: An `optuna.Trial` or `dict[str, Any]` for hyperparameter search.\n* **ignore_keys_for_eval**: A `list[str]` of keys in the model output to ignore when gathering predictions during evaluation.",
      "retrieval_latency_ms": 323.9979170029983,
      "generation_latency_ms": 101539.50075001922,
      "prompt_tokens": 2376,
      "completion_tokens": 698,
      "source_hit": 1.0,
      "estimated_api_cost_usd": null,
      "faithfulness": 1.0,
      "answer_relevancy": 0.8267508134006029,
      "answer_correctness": 0.8481261947765433
    }
  ]
}